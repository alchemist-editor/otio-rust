The ten edit operations are in the core and in the C ABI, so every SDK below has them, but the Python bindings do not reach them yet: there is no `opentimelineio.algorithms` module, and `insert`, `overwrite` and the rest are not on any class. Upstream's Python exposes them, so this is a gap in the bindings rather than a difference in design.
