// Code generated by otio-sdk-gen from crates/otio-capi. DO NOT EDIT.

#pragma once

#include <cstdint>

namespace otio {

/// Whether a timecode is written in drop-frame form.
enum class DropFrame : std::int32_t {
    /// Use drop-frame form if the rate is a drop-frame rate.
    INFER_FROM_RATE = 0,

    /// Never use drop-frame form.
    FORCE_NO = 1,

    /// Use drop-frame form, failing if the rate has none.
    FORCE_YES = 2,
};

/// The name the C interface spells a value by.
inline const char *to_string(DropFrame value) {
    switch (value) {
    case DropFrame::INFER_FROM_RATE:
        return "OTIO_DROP_FRAME_INFER_FROM_RATE";
    case DropFrame::FORCE_NO:
        return "OTIO_DROP_FRAME_FORCE_NO";
    case DropFrame::FORCE_YES:
        return "OTIO_DROP_FRAME_FORCE_YES";
    }
    return "";
}

/// Which system's conventions an EDL is written for.
enum class EDLStyle : std::int32_t {
    /// Avid Media Composer.
    AVID = 0,

    /// Nucoda.
    NUCODA = 1,

    /// Adobe Premiere Pro.
    PREMIERE = 2,
};

/// The name the C interface spells a value by.
inline const char *to_string(EDLStyle value) {
    switch (value) {
    case EDLStyle::AVID:
        return "OTIO_EDL_STYLE_AVID";
    case EDLStyle::NUCODA:
        return "OTIO_EDL_STYLE_NUCODA";
    case EDLStyle::PREMIERE:
        return "OTIO_EDL_STYLE_PREMIERE";
    }
    return "";
}

/// A file format this library reads and writes.
enum class Format : std::int32_t {
    /// OpenTimelineIO's own JSON, the `.otio` file.
    OTIO_JSON = 0,

    /// Avid Log Exchange, the `.ale` file.
    ALE = 1,

    /// CMX 3600 EDL, the `.edl` file.
    CMX_3600 = 2,

    /// Final Cut Pro 7 XML, the `.xml` file.
    FCP7_XML = 3,

    /// Final Cut Pro X XML, the `.fcpxml` file.
    FCPX_XML = 4,

    /// The Advanced Authoring Format, the `.aaf` file.
    AAF = 5,
};

/// The name the C interface spells a value by.
inline const char *to_string(Format value) {
    switch (value) {
    case Format::OTIO_JSON:
        return "OTIO_FORMAT_OTIO_JSON";
    case Format::ALE:
        return "OTIO_FORMAT_ALE";
    case Format::CMX_3600:
        return "OTIO_FORMAT_CMX_3600";
    case Format::FCP7_XML:
        return "OTIO_FORMAT_FCP7_XML";
    case Format::FCPX_XML:
        return "OTIO_FORMAT_FCPX_XML";
    case Format::AAF:
        return "OTIO_FORMAT_AAF";
    }
    return "";
}

/// What to show for a frame an image sequence is missing.
enum class MissingFramePolicy : std::int32_t {
    /// Treat the gap as an error.
    ERROR = 0,

    /// Show black.
    BLACK = 1,

    /// Hold the previous frame.
    HOLD = 2,
};

/// The name the C interface spells a value by.
inline const char *to_string(MissingFramePolicy value) {
    switch (value) {
    case MissingFramePolicy::ERROR:
        return "OTIO_MISSING_FRAME_ERROR";
    case MissingFramePolicy::BLACK:
        return "OTIO_MISSING_FRAME_BLACK";
    case MissingFramePolicy::HOLD:
        return "OTIO_MISSING_FRAME_HOLD";
    }
    return "";
}

/// What to do about a transition at the very start or end of a track when
/// asking for its neighbours.
enum class NeighborGapPolicy : std::int32_t {
    /// Report no neighbour, which is the literal truth.
    NEVER = 0,

    /// Report a gap the length of the transition's overhang.
    AROUND_TRANSITIONS = 1,
};

/// The name the C interface spells a value by.
inline const char *to_string(NeighborGapPolicy value) {
    switch (value) {
    case NeighborGapPolicy::NEVER:
        return "OTIO_NEIGHBOR_GAP_NEVER";
    case NeighborGapPolicy::AROUND_TRANSITIONS:
        return "OTIO_NEIGHBOR_GAP_AROUND_TRANSITIONS";
    }
    return "";
}

/// What kind of object a handle names.
///
/// These follow the schemas of upstream OpenTimelineIO 0.19.0. An object
/// whose schema this library does not know reads as
/// `NodeKind::UNKNOWN_SCHEMA` and keeps its fields verbatim.
enum class NodeKind : std::int32_t {
    /// A bare `Item`.
    ITEM = 0,

    /// A `Clip`.
    CLIP = 1,

    /// A `Gap`.
    GAP = 2,

    /// A `Track`.
    TRACK = 3,

    /// A `Stack`.
    STACK = 4,

    /// A `Timeline`.
    TIMELINE = 5,

    /// A `Transition`.
    TRANSITION = 6,

    /// A `Marker`.
    MARKER = 7,

    /// An `Effect`.
    EFFECT = 8,

    /// A `TimeEffect`.
    TIME_EFFECT = 9,

    /// A `LinearTimeWarp`.
    LINEAR_TIME_WARP = 10,

    /// A `FreezeFrame`.
    FREEZE_FRAME = 11,

    /// An `ExternalReference`.
    EXTERNAL_REFERENCE = 12,

    /// A `MissingReference`.
    MISSING_REFERENCE = 13,

    /// A `GeneratorReference`.
    GENERATOR_REFERENCE = 14,

    /// An `ImageSequenceReference`.
    IMAGE_SEQUENCE_REFERENCE = 15,

    /// A `SerializableCollection`.
    SERIALIZABLE_COLLECTION = 16,

    /// A `SerializableObject`.
    SERIALIZABLE_OBJECT = 17,

    /// A `SerializableObjectWithMetadata`.
    SERIALIZABLE_OBJECT_WITH_METADATA = 18,

    /// A `Composable`.
    COMPOSABLE = 19,

    /// A bare `Composition`.
    COMPOSITION = 20,

    /// A bare `MediaReference`.
    MEDIA_REFERENCE = 21,

    /// An object whose schema this library does not know.
    UNKNOWN_SCHEMA = 22,

    /// A schema added to the core since this ABI was written.
    ///
    /// Nothing produces this today. It exists so that a core that grows a new
    /// object reports something honest to a caller built against this header
    /// rather than a kind that means something else.
    OTHER = 23,
};

/// The name the C interface spells a value by.
inline const char *to_string(NodeKind value) {
    switch (value) {
    case NodeKind::ITEM:
        return "OTIO_NODE_KIND_ITEM";
    case NodeKind::CLIP:
        return "OTIO_NODE_KIND_CLIP";
    case NodeKind::GAP:
        return "OTIO_NODE_KIND_GAP";
    case NodeKind::TRACK:
        return "OTIO_NODE_KIND_TRACK";
    case NodeKind::STACK:
        return "OTIO_NODE_KIND_STACK";
    case NodeKind::TIMELINE:
        return "OTIO_NODE_KIND_TIMELINE";
    case NodeKind::TRANSITION:
        return "OTIO_NODE_KIND_TRANSITION";
    case NodeKind::MARKER:
        return "OTIO_NODE_KIND_MARKER";
    case NodeKind::EFFECT:
        return "OTIO_NODE_KIND_EFFECT";
    case NodeKind::TIME_EFFECT:
        return "OTIO_NODE_KIND_TIME_EFFECT";
    case NodeKind::LINEAR_TIME_WARP:
        return "OTIO_NODE_KIND_LINEAR_TIME_WARP";
    case NodeKind::FREEZE_FRAME:
        return "OTIO_NODE_KIND_FREEZE_FRAME";
    case NodeKind::EXTERNAL_REFERENCE:
        return "OTIO_NODE_KIND_EXTERNAL_REFERENCE";
    case NodeKind::MISSING_REFERENCE:
        return "OTIO_NODE_KIND_MISSING_REFERENCE";
    case NodeKind::GENERATOR_REFERENCE:
        return "OTIO_NODE_KIND_GENERATOR_REFERENCE";
    case NodeKind::IMAGE_SEQUENCE_REFERENCE:
        return "OTIO_NODE_KIND_IMAGE_SEQUENCE_REFERENCE";
    case NodeKind::SERIALIZABLE_COLLECTION:
        return "OTIO_NODE_KIND_SERIALIZABLE_COLLECTION";
    case NodeKind::SERIALIZABLE_OBJECT:
        return "OTIO_NODE_KIND_SERIALIZABLE_OBJECT";
    case NodeKind::SERIALIZABLE_OBJECT_WITH_METADATA:
        return "OTIO_NODE_KIND_SERIALIZABLE_OBJECT_WITH_METADATA";
    case NodeKind::COMPOSABLE:
        return "OTIO_NODE_KIND_COMPOSABLE";
    case NodeKind::COMPOSITION:
        return "OTIO_NODE_KIND_COMPOSITION";
    case NodeKind::MEDIA_REFERENCE:
        return "OTIO_NODE_KIND_MEDIA_REFERENCE";
    case NodeKind::UNKNOWN_SCHEMA:
        return "OTIO_NODE_KIND_UNKNOWN_SCHEMA";
    case NodeKind::OTHER:
        return "OTIO_NODE_KIND_OTHER";
    }
    return "";
}

/// Which clock a three- or four-point edit lines its media up against.
enum class ReferencePoint : std::int32_t {
    /// Use the media's own timing, and take as much of it as fits.
    SOURCE = 0,

    /// Line the media up against the track, trimming it to the gap.
    SEQUENCE = 1,

    /// Stretch or squeeze the media to fill the gap exactly.
    FIT = 2,
};

/// The name the C interface spells a value by.
inline const char *to_string(ReferencePoint value) {
    switch (value) {
    case ReferencePoint::SOURCE:
        return "OTIO_REFERENCE_POINT_SOURCE";
    case ReferencePoint::SEQUENCE:
        return "OTIO_REFERENCE_POINT_SEQUENCE";
    case ReferencePoint::FIT:
        return "OTIO_REFERENCE_POINT_FIT";
    }
    return "";
}

/// What a call did, or why it could not.
///
/// `Status::OK` is zero, so `if (otio_...(...)) { /* failed */ }` reads the
/// way a C programmer expects.
enum class Status : std::int32_t {
    /// The call succeeded.
    OK = 0,

    /// A pointer argument that may not be absent was absent.
    NULL_POINTER = 1,

    /// A string argument was not valid UTF-8.
    INVALID_UTF8 = 2,

    /// The call succeeded, and the answer is that there is no value.
    ///
    /// An item with no `source_range` and a clip with no active media reference
    /// both report this. It is not an error.
    NO_VALUE = 3,

    /// A node handle named an object that no longer exists.
    STALE_HANDLE = 4,

    /// An argument was outside the range the call accepts.
    INVALID_ARGUMENT = 5,

    /// The document could not answer the question asked of it.
    ///
    /// Asking a marker for its duration, or a track for a child it does not
    /// hold, lands here, and the message that comes with it says which.
    CORE_ERROR = 6,

    /// A timecode or time string could not be read or written.
    TIME_ERROR = 7,

    /// A file was not valid for the format it was read as.
    PARSE_ERROR = 8,

    /// The document holds something the target format cannot express.
    UNSUPPORTED = 9,

    /// A file could not be read from or written to disk.
    IO_ERROR = 10,

    /// A panic in the Rust core was caught at the boundary.
    ///
    /// The library is left in an unspecified state; a caller that sees this
    /// should stop using the document it was working on.
    PANIC = 11,
};

/// The name the C interface spells a value by.
inline const char *to_string(Status value) {
    switch (value) {
    case Status::OK:
        return "OTIO_STATUS_OK";
    case Status::NULL_POINTER:
        return "OTIO_STATUS_NULL_POINTER";
    case Status::INVALID_UTF8:
        return "OTIO_STATUS_INVALID_UTF8";
    case Status::NO_VALUE:
        return "OTIO_STATUS_NO_VALUE";
    case Status::STALE_HANDLE:
        return "OTIO_STATUS_STALE_HANDLE";
    case Status::INVALID_ARGUMENT:
        return "OTIO_STATUS_INVALID_ARGUMENT";
    case Status::CORE_ERROR:
        return "OTIO_STATUS_CORE_ERROR";
    case Status::TIME_ERROR:
        return "OTIO_STATUS_TIME_ERROR";
    case Status::PARSE_ERROR:
        return "OTIO_STATUS_PARSE_ERROR";
    case Status::UNSUPPORTED:
        return "OTIO_STATUS_UNSUPPORTED";
    case Status::IO_ERROR:
        return "OTIO_STATUS_IO_ERROR";
    case Status::PANIC:
        return "OTIO_STATUS_PANIC";
    }
    return "";
}

/// What kind of value sits at a path.
enum class ValueKind : std::int32_t {
    /// JSON `null`.
    VALUE_NULL = 0,

    /// A boolean.
    BOOL = 1,

    /// A signed integer.
    INT = 2,

    /// An unsigned integer too large to be signed.
    UINT = 3,

    /// A number.
    DOUBLE = 4,

    /// A string.
    STRING = 5,

    /// A `RationalTime`.
    RATIONAL_TIME = 6,

    /// A `TimeRange`.
    TIME_RANGE = 7,

    /// A `TimeTransform`.
    TIME_TRANSFORM = 8,

    /// A `Color`.
    COLOR = 9,

    /// A `V2d`.
    V2D = 10,

    /// A `Box2d`.
    BOX2D = 11,

    /// An array.
    VECTOR = 12,

    /// A dictionary.
    DICTIONARY = 13,

    /// A whole OTIO object, named by a handle.
    OBJECT = 14,

    /// A kind added to the core since this ABI was written.
    ///
    /// Nothing produces this today. It exists so that a core that grows a new
    /// metadata type reports something honest rather than a kind that means
    /// something else.
    OTHER = 15,
};

/// The name the C interface spells a value by.
inline const char *to_string(ValueKind value) {
    switch (value) {
    case ValueKind::VALUE_NULL:
        return "OTIO_VALUE_NULL";
    case ValueKind::BOOL:
        return "OTIO_VALUE_BOOL";
    case ValueKind::INT:
        return "OTIO_VALUE_INT";
    case ValueKind::UINT:
        return "OTIO_VALUE_UINT";
    case ValueKind::DOUBLE:
        return "OTIO_VALUE_DOUBLE";
    case ValueKind::STRING:
        return "OTIO_VALUE_STRING";
    case ValueKind::RATIONAL_TIME:
        return "OTIO_VALUE_RATIONAL_TIME";
    case ValueKind::TIME_RANGE:
        return "OTIO_VALUE_TIME_RANGE";
    case ValueKind::TIME_TRANSFORM:
        return "OTIO_VALUE_TIME_TRANSFORM";
    case ValueKind::COLOR:
        return "OTIO_VALUE_COLOR";
    case ValueKind::V2D:
        return "OTIO_VALUE_V2D";
    case ValueKind::BOX2D:
        return "OTIO_VALUE_BOX2D";
    case ValueKind::VECTOR:
        return "OTIO_VALUE_VECTOR";
    case ValueKind::DICTIONARY:
        return "OTIO_VALUE_DICTIONARY";
    case ValueKind::OBJECT:
        return "OTIO_VALUE_OBJECT";
    case ValueKind::OTHER:
        return "OTIO_VALUE_OTHER";
    }
    return "";
}

}  // namespace otio
