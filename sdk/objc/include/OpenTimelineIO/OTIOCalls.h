// Code generated by otio-sdk-gen from crates/otio-capi. DO NOT EDIT.

#pragma once

#import <Foundation/Foundation.h>

#import "OpenTimelineIO/OTIOEnums.h"
#import "OpenTimelineIO/OTIORuntime.h"
#import "OpenTimelineIO/OTIOSchema.h"
#import "OpenTimelineIO/OTIOValues.h"

NS_ASSUME_NONNULL_BEGIN

#pragma mark - Functions

/// Returns the format that claims a filename suffix, such as `"edl"`.
///
/// The suffix is matched without its dot and without regard to case.
/// Reports `OTIOStatusNoValue` for a suffix no format claims.
///
/// C: `otio_format_from_suffix`
BOOL OTIOFormatFromSuffix(NSString *suffix, OTIOFormat *outFormat, NSError **error);

/// Returns an adapter's name, as upstream's plugin manifest spells it.
///
/// The string is static and needs no freeing.
///
/// C: `otio_format_name`
NSString *OTIOFormatName(OTIOFormat format);

/// The tolerance, in seconds, that the range predicates use by default.
///
/// Upstream's C++ takes this as a default argument; C has none, so the
/// value is a call rather than a constant in the header, which keeps the
/// two from drifting apart.
///
/// C: `otio_default_epsilon_s`
double OTIODefaultEpsilonS(void);

/// Returns the indentation upstream's Python bindings write by default.
///
/// C: `otio_default_indent`
NSUInteger OTIODefaultIndent(void);

/// Returns the library's version, as `"MAJOR.MINOR.PATCH"`.
///
/// The string is static and needs no freeing.
///
/// C: `otio_version`
NSString *OTIOVersion(void);

/// Returns whether a rate is a drop-frame rate.
///
/// C: `otio_is_drop_frame_rate`
BOOL OTIOIsDropFrameRate(double rate);

/// Returns whether a rate is one SMPTE timecode is defined for.
///
/// C: `otio_is_smpte_timecode_rate`
BOOL OTIOIsSMPTETimecodeRate(double rate);

/// Returns the SMPTE timecode rate closest to a rate.
///
/// C: `otio_nearest_smpte_timecode_rate`
double OTIONearestSMPTETimecodeRate(double rate);

/// Returns the sum of two times, at the higher of the two rates.
///
/// C: `otio_rational_time_add`
OTIORationalTime OTIORationalTimeAdd(OTIORationalTime left, OTIORationalTime right);

/// Returns whether two times are within `delta` of each other.
///
/// C: `otio_rational_time_almost_equal`
BOOL OTIORationalTimeAlmostEquals(OTIORationalTime left, OTIORationalTime right, double delta);

/// Returns the time rounded towards positive infinity.
///
/// C: `otio_rational_time_ceil`
OTIORationalTime OTIORationalTimeCeil(OTIORationalTime time);

/// Orders two times: -1 if `left` is earlier, 1 if later, 0 if the same.
///
/// A comparison involving a NaN has no answer, and reports 0.
///
/// C: `otio_rational_time_compare`
int32_t OTIORationalTimeCompare(OTIORationalTime left, OTIORationalTime right);

/// Returns how long it is from one instant to another, the end excluded.
///
/// C: `otio_rational_time_duration_from_start_end_time`
OTIORationalTime OTIORationalTimeDurationFromStartEndTime(OTIORationalTime startTime, OTIORationalTime endTimeExclusive);

/// Returns how long it is from one instant to another, the end included.
///
/// C: `otio_rational_time_duration_from_start_end_time_inclusive`
OTIORationalTime OTIORationalTimeDurationFromStartEndTimeInclusive(OTIORationalTime startTime, OTIORationalTime endTimeInclusive);

/// Returns whether two times are the same instant.
///
/// Times at different rates that name the same instant compare equal;
/// this is upstream's `==`, not a field-by-field comparison. For that,
/// use `OTIORationalTimeStrictlyEquals`.
///
/// C: `otio_rational_time_equal`
BOOL OTIORationalTimeEquals(OTIORationalTime left, OTIORationalTime right);

/// Returns the time rounded towards negative infinity.
///
/// C: `otio_rational_time_floor`
OTIORationalTime OTIORationalTimeFloor(OTIORationalTime time);

/// Builds a time from a frame number at a rate.
///
/// C: `otio_rational_time_from_frames`
OTIORationalTime OTIORationalTimeFromFrames(double frame, double rate);

/// Builds a time from a number of seconds, at a rate of one.
///
/// C: `otio_rational_time_from_seconds`
OTIORationalTime OTIORationalTimeFromSeconds(double seconds);

/// Builds a time from a number of seconds at a rate.
///
/// C: `otio_rational_time_from_seconds_at_rate`
OTIORationalTime OTIORationalTimeFromSecondsAtRate(double seconds, double rate);

/// Reads a time from a `[-]HH:MM:SS.sss` time string at a rate.
///
/// C: `otio_rational_time_from_time_string`
BOOL OTIORationalTimeFromTimeString(NSString *timeString, double rate, OTIORationalTime *outTime, NSError **error);

/// Reads a time from a `HH:MM:SS:FF` timecode at a rate.
///
/// C: `otio_rational_time_from_timecode`
BOOL OTIORationalTimeFromTimecode(NSString *timecode, double rate, OTIORationalTime *outTime, NSError **error);

/// Returns whether a time is usable: both parts finite and the rate
/// positive.
///
/// C: `otio_rational_time_is_valid`
BOOL OTIORationalTimeIsValid(OTIORationalTime time);

/// Returns the time with its sign flipped.
///
/// C: `otio_rational_time_negate`
OTIORationalTime OTIORationalTimeNegate(OTIORationalTime time);

/// Returns the same instant expressed at another rate.
///
/// C: `otio_rational_time_rescaled_to`
OTIORationalTime OTIORationalTimeRescaledTo(OTIORationalTime time, double rate);

/// Returns the same instant expressed at another time's rate.
///
/// C: `otio_rational_time_rescaled_to_time`
OTIORationalTime OTIORationalTimeRescaledToTime(OTIORationalTime time, OTIORationalTime other);

/// Returns the time rounded to the nearest whole value, halves away from
/// zero.
///
/// C: `otio_rational_time_round`
OTIORationalTime OTIORationalTimeRound(OTIORationalTime time);

/// Returns whether two times have the same value and the same rate.
///
/// C: `otio_rational_time_strictly_equal`
BOOL OTIORationalTimeStrictlyEquals(OTIORationalTime left, OTIORationalTime right);

/// Returns the difference of two times, at the higher of the two rates.
///
/// C: `otio_rational_time_subtract`
OTIORationalTime OTIORationalTimeSubtract(OTIORationalTime left, OTIORationalTime right);

/// Returns the frame number this time falls on.
///
/// C: `otio_rational_time_to_frames`
int32_t OTIORationalTimeToFrames(OTIORationalTime time);

/// Returns the frame number this time falls on at another rate.
///
/// C: `otio_rational_time_to_frames_at_rate`
int32_t OTIORationalTimeToFramesAtRate(OTIORationalTime time, double rate);

/// Writes a time as a timecode, rounding to the nearest frame first.
///
/// C: `otio_rational_time_to_nearest_timecode_at`
NSString *_Nullable OTIORationalTimeToNearestTimecodeAt(OTIORationalTime time, double rate, OTIODropFrame dropFrame, NSError **error);

/// Returns the time in seconds.
///
/// C: `otio_rational_time_to_seconds`
double OTIORationalTimeToSeconds(OTIORationalTime time);

/// Writes a time as a `HH:MM:SS.sss` time string.
///
/// C: `otio_rational_time_to_time_string`
NSString *_Nullable OTIORationalTimeToTimeString(OTIORationalTime time, NSError **error);

/// Writes a time as a timecode at its own rate.
///
/// C: `otio_rational_time_to_timecode`
NSString *_Nullable OTIORationalTimeToTimecode(OTIORationalTime time, NSError **error);

/// Writes a time as a timecode at a given rate and drop-frame setting.
///
/// C: `otio_rational_time_to_timecode_at`
NSString *_Nullable OTIORationalTimeToTimecodeAt(OTIORationalTime time, double rate, OTIODropFrame dropFrame, NSError **error);

/// Returns what this time's value would be at another rate.
///
/// C: `otio_rational_time_value_rescaled_to`
double OTIORationalTimeValueRescaledTo(OTIORationalTime time, double rate);

/// Returns whether this range ends before another begins.
///
/// C: `otio_time_range_before_range`
BOOL OTIOTimeRangeBeforeRange(OTIOTimeRange range, OTIOTimeRange other, double epsilonS);

/// Returns whether this range ends before an instant.
///
/// C: `otio_time_range_before_time`
BOOL OTIOTimeRangeBeforeTime(OTIOTimeRange range, OTIORationalTime time, double epsilonS);

/// Returns whether another range starts where this one does.
///
/// C: `otio_time_range_begins_range`
BOOL OTIOTimeRangeBeginsRange(OTIOTimeRange range, OTIOTimeRange other, double epsilonS);

/// Returns whether this range starts at an instant.
///
/// C: `otio_time_range_begins_time`
BOOL OTIOTimeRangeBeginsTime(OTIOTimeRange range, OTIORationalTime time, double epsilonS);

/// Returns the range pulled inside this one, where it lies outside it.
///
/// C: `otio_time_range_clamped_range`
OTIOTimeRange OTIOTimeRangeClampedRange(OTIOTimeRange range, OTIOTimeRange other);

/// Returns the instant pulled inside the range, if it lies outside it.
///
/// C: `otio_time_range_clamped_time`
OTIORationalTime OTIOTimeRangeClampedTime(OTIOTimeRange range, OTIORationalTime time);

/// Returns whether another range falls entirely inside this one.
///
/// C: `otio_time_range_contains_range`
BOOL OTIOTimeRangeContainsRange(OTIOTimeRange range, OTIOTimeRange other, double epsilonS);

/// Returns whether an instant falls inside the range, the end excluded.
///
/// C: `otio_time_range_contains_time`
BOOL OTIOTimeRangeContainsTime(OTIOTimeRange range, OTIORationalTime time);

/// Returns the range with its duration lengthened.
///
/// C: `otio_time_range_duration_extended_by`
OTIOTimeRange OTIOTimeRangeDurationExtendedBy(OTIOTimeRange range, OTIORationalTime by);

/// Returns the instant just after the range's end.
///
/// C: `otio_time_range_end_time_exclusive`
OTIORationalTime OTIOTimeRangeEndTimeExclusive(OTIOTimeRange range);

/// Returns the last instant the range covers.
///
/// C: `otio_time_range_end_time_inclusive`
OTIORationalTime OTIOTimeRangeEndTimeInclusive(OTIOTimeRange range);

/// Returns the smallest range covering both of two ranges.
///
/// C: `otio_time_range_extended_by`
OTIOTimeRange OTIOTimeRangeExtendedBy(OTIOTimeRange range, OTIOTimeRange other);

/// Returns whether another range ends where this one does.
///
/// C: `otio_time_range_finishes_range`
BOOL OTIOTimeRangeFinishesRange(OTIOTimeRange range, OTIOTimeRange other, double epsilonS);

/// Returns whether this range ends at an instant.
///
/// C: `otio_time_range_finishes_time`
BOOL OTIOTimeRangeFinishesTime(OTIOTimeRange range, OTIORationalTime time, double epsilonS);

/// Builds a range from its start and the instant after its end.
///
/// C: `otio_time_range_from_start_end_time`
OTIOTimeRange OTIOTimeRangeFromStartEndTime(OTIORationalTime startTime, OTIORationalTime endTimeExclusive);

/// Builds a range from its start and its last instant.
///
/// C: `otio_time_range_from_start_end_time_inclusive`
OTIOTimeRange OTIOTimeRangeFromStartEndTimeInclusive(OTIORationalTime startTime, OTIORationalTime endTimeInclusive);

/// Returns whether two ranges share more than a single boundary instant.
///
/// C: `otio_time_range_intersects`
BOOL OTIOTimeRangeIntersects(OTIOTimeRange range, OTIOTimeRange other, double epsilonS);

/// Returns whether a range is usable: valid times and a duration of at
/// least zero.
///
/// C: `otio_time_range_is_valid`
BOOL OTIOTimeRangeIsValid(OTIOTimeRange range);

/// Returns whether this range ends exactly where another begins.
///
/// C: `otio_time_range_meets`
BOOL OTIOTimeRangeMeets(OTIOTimeRange range, OTIOTimeRange other, double epsilonS);

/// Returns whether two ranges share any time at all.
///
/// C: `otio_time_range_overlaps_range`
BOOL OTIOTimeRangeOverlapsRange(OTIOTimeRange range, OTIOTimeRange other, double epsilonS);

/// Returns whether an instant falls inside the range, the ends included.
///
/// C: `otio_time_range_overlaps_time`
BOOL OTIOTimeRangeOverlapsTime(OTIOTimeRange range, OTIORationalTime time);

/// Returns the transform applied to a span.
///
/// C: `otio_time_transform_applied_to_range`
OTIOTimeRange OTIOTimeTransformAppliedToRange(OTIOTimeTransform transform, OTIOTimeRange range);

/// Returns the transform applied to an instant.
///
/// C: `otio_time_transform_applied_to_time`
OTIORationalTime OTIOTimeTransformAppliedToTime(OTIOTimeTransform transform, OTIORationalTime time);

/// Returns the transform applied to another transform.
///
/// C: `otio_time_transform_applied_to_transform`
OTIOTimeTransform OTIOTimeTransformAppliedToTransform(OTIOTimeTransform transform, OTIOTimeTransform other);

/// Reads a document from the bytes of a file in some format.
///
/// `options` may be nil for the format's usual behaviour.
///
/// C: `otio_read_from_bytes`
OTIOSerializableObject *_Nullable OTIOReadFromBytes(OTIOFormat format, NSData *data, const OTIOReadOptions *_Nullable options, NSError **error);

/// Reads a document from a file on disk in some format.
///
/// A nil options means none.
///
/// C: `otio_read_from_file`
OTIOSerializableObject *_Nullable OTIOReadFromFile(OTIOFormat format, NSString *path, const OTIOReadOptions *_Nullable options, NSError **error);

/// Returns the defaults, for a caller that wants to change one field.
///
/// C: `otio_read_options_default`
OTIOReadOptions OTIOReadOptionsDefault(void);

/// Returns the defaults, for a caller that wants to change one field.
///
/// C: `otio_write_options_default`
OTIOWriteOptions OTIOWriteOptionsDefault(void);

/// Writes a document as the bytes of a file in some format.
///
/// The buffer is NUL-terminated, so a text format's output can be used as
/// a C string; `len` is what matters for a binary one.
///
/// A nil options means none.
///
/// C: `otio_write_to_bytes`
NSData *_Nullable OTIOWriteToBytes(OTIOFormat format, OTIOSerializableObject *root, const OTIOWriteOptions *_Nullable options, NSError **error);

/// Writes a document to a file on disk in some format.
///
/// A nil options means none.
///
/// C: `otio_write_to_file`
BOOL OTIOWriteToFile(OTIOFormat format, OTIOSerializableObject *root, NSString *path, const OTIOWriteOptions *_Nullable options, NSError **error);

/// Collapses a stack's tracks into one, top layer winning where it is
/// visible.
///
/// C: `otio_algorithm_flatten_stack`
OTIOSerializableObject *_Nullable OTIOFlattenStack(OTIOSerializableObject *stack, NSError **error);

/// Collapses a list of tracks into one, lowest first.
///
/// A nil tracks means none.
///
/// C: `otio_algorithm_flatten_tracks`
OTIOSerializableObject *_Nullable OTIOFlattenTracks(NSArray<OTIOSerializableObject *> *tracks, NSError **error);

/// Returns a copy of a track holding only what falls inside a span.
///
/// The copy is added to the same document and has no parent.
///
/// C: `otio_algorithm_track_trimmed_to_range`
OTIOSerializableObject *_Nullable OTIOTrackTrimmedToRange(OTIOSerializableObject *track, OTIOTimeRange trimRange, NSError **error);

/// Reads a document from OTIO JSON.
///
/// C: `otio_document_from_json`
OTIOSerializableObject *_Nullable OTIOFromJSON(NSString *json, NSError **error);

/// Reads a document from a `.otio` file on disk.
///
/// C: `otio_document_read_from_file`
OTIOSerializableObject *_Nullable OTIOReadOTIOFile(NSString *path, NSError **error);

/// Writes a document to a `.otio` file on disk.
///
/// C: `otio_document_write_to_file`
BOOL OTIOWriteOTIOFile(OTIOSerializableObject *root, NSString *path, NSUInteger indent, NSError **error);

/// Drops an item into a gap on a track, fitting it as the reference point
/// says.
///
/// C: `otio_edit_fill`
BOOL OTIOFill(OTIOSerializableObject *item, OTIOSerializableObject *track, OTIORationalTime trackTime, OTIOReferencePoint referencePoint, NSError **error);

/// Inserts an item at an instant, pushing what follows later.
///
/// A nil fillTemplate means none.
///
/// C: `otio_edit_insert`
BOOL OTIOInsert(OTIOSerializableObject *item, OTIOSerializableObject *composition, OTIORationalTime time, BOOL removeTransitions, OTIOSerializableObject *_Nullable fillTemplate, NSError **error);

/// Lays an item over a span of a composition, replacing what was there.
///
/// `fill_template` is the item to fill any gap the edit opens with, or
/// `none` for a plain gap.
///
/// A nil fillTemplate means none.
///
/// C: `otio_edit_overwrite`
BOOL OTIOOverwrite(OTIOSerializableObject *item, OTIOSerializableObject *composition, OTIOTimeRange range, BOOL removeTransitions, OTIOSerializableObject *_Nullable fillTemplate, NSError **error);

/// Takes whatever sits at an instant out of a composition.
///
/// With `fill` set, a gap takes its place; without, what follows moves
/// up.
///
/// A nil fillTemplate means none.
///
/// C: `otio_edit_remove`
BOOL OTIORemove(OTIOSerializableObject *composition, OTIORationalTime time, BOOL fill, OTIOSerializableObject *_Nullable fillTemplate, NSError **error);

/// Moves an item's in and out points, sliding everything after it.
///
/// C: `otio_edit_ripple`
BOOL OTIORipple(OTIOSerializableObject *item, OTIORationalTime deltaIn, OTIORationalTime deltaOut, NSError **error);

/// Moves the cut between an item and its neighbour.
///
/// C: `otio_edit_roll`
BOOL OTIORoll(OTIOSerializableObject *item, OTIORationalTime deltaIn, OTIORationalTime deltaOut, NSError **error);

/// Cuts whatever sits at an instant into two.
///
/// C: `otio_edit_slice`
BOOL OTIOSlice(OTIOSerializableObject *composition, OTIORationalTime time, BOOL removeTransitions, NSError **error);

/// Moves an item along its track, taking the time from its neighbours.
///
/// C: `otio_edit_slide`
BOOL OTIOSlide(OTIOSerializableObject *item, OTIORationalTime delta, NSError **error);

/// Moves the media inside an item without moving the item.
///
/// C: `otio_edit_slip`
BOOL OTIOSlip(OTIOSerializableObject *item, OTIORationalTime delta, NSError **error);

/// Moves an item's in and out points without moving its neighbours.
///
/// A nil fillTemplate means none.
///
/// C: `otio_edit_trim`
BOOL OTIOTrim(OTIOSerializableObject *item, OTIORationalTime deltaIn, OTIORationalTime deltaOut, OTIOSerializableObject *_Nullable fillTemplate, NSError **error);

#pragma mark - Objects

@interface OTIOClip (OTIOGenerated)

/// Returns which of a clip's media references is in use.
///
/// C: `otio_clip_active_media_reference_key`
- (nullable NSString *)activeMediaReferenceKey:(NSError **)error;

/// Returns one of a clip's media references.
///
/// A nil `key` means the active one. Reports `OTIOStatusNoValue` if the
/// key names nothing, which for the active key is a clip with no media.
///
/// C: `otio_clip_media_reference`
- (nullable OTIOSerializableObject *)mediaReference:(NSString *_Nullable)key error:(NSError **)error;

/// Returns how many media references a clip holds.
///
/// C: `otio_clip_media_reference_count`
- (BOOL)getMediaReferenceCount:(NSUInteger *)outCount error:(NSError **)error;

/// Returns the key of one of a clip's media references.
///
/// The references are ordered by key, so walking the indices walks them
/// in a stable order.
///
/// C: `otio_clip_media_reference_key_at`
- (nullable NSString *)mediaReferenceKeyAt:(NSUInteger)index error:(NSError **)error;

/// Creates a clip. `name` may be nil for an unnamed one.
///
/// C: `otio_clip_new`
+ (nullable OTIOClip *)clipWithName:(NSString *_Nullable)name error:(NSError **)error;

/// Removes one of a clip's media references, and returns it.
///
/// Where there is nothing to report this fails with OTIOStatusNoValue,
/// which OTIOIsNoValue tells apart from a real failure.
///
/// C: `otio_clip_remove_media_reference`
- (nullable OTIOSerializableObject *)removeMediaReference:(NSString *)key error:(NSError **)error;

/// Sets which of a clip's media references is in use.
///
/// C: `otio_clip_set_active_media_reference_key`
- (BOOL)setActiveMediaReferenceKey:(NSString *)key error:(NSError **)error;

/// Sets one of a clip's media references, adding it if the key is new.
///
/// C: `otio_clip_set_media_reference`
- (BOOL)setMediaReference:(NSString *)key reference:(OTIOSerializableObject *)reference error:(NSError **)error;
@end

@interface OTIOComposable (OTIOGenerated)

/// Creates a composable: something that sits in a composition and nothing
/// more.
///
/// A nil name means none.
///
/// C: `otio_composable_new`
+ (nullable OTIOComposable *)composableWithName:(NSString *_Nullable)name error:(NSError **)error;
@end

@interface OTIOComposition (OTIOGenerated)

/// Adds a child to the end of a composition.
///
/// C: `otio_composition_append_child`
- (BOOL)appendChild:(OTIOSerializableObject *)child error:(NSError **)error;

/// Returns the child of a composition that covers an instant.
///
/// `shallow` stops at the composition's own children rather than
/// descending into nested ones. Reports `OTIOStatusNoValue` when nothing
/// covers it.
///
/// C: `otio_composition_child_at_time`
- (nullable OTIOSerializableObject *)childAtTime:(OTIORationalTime)time shallow:(BOOL)shallow error:(NSError **)error;

/// Returns the children of a composition that touch a span.
///
/// C: `otio_composition_children_in_range`
- (nullable NSArray<OTIOSerializableObject *> *)childrenInRange:(OTIOTimeRange)searchRange error:(NSError **)error;

/// Removes every child of a composition, and returns them.
///
/// C: `otio_composition_clear_children`
- (nullable NSArray<OTIOSerializableObject *> *)clearChildren:(NSError **)error;

/// Removes a child by handle.
///
/// C: `otio_composition_detach_child`
- (BOOL)detachChild:(OTIOSerializableObject *)child error:(NSError **)error;

/// Returns every object of a kind at or below a composition.
///
/// A nil `search_range` searches all of it. `shallow` stops the walk at
/// the composition's own children.
///
/// C: `otio_composition_find_children_of_kind`
- (nullable NSArray<OTIOSerializableObject *> *)findChildrenOfKind:(OTIONodeKind)kind searchRange:(const OTIOTimeRange *_Nullable)searchRange shallow:(BOOL)shallow error:(NSError **)error;

/// Returns how much unused media a child has on each side.
///
/// C: `otio_composition_handles_of_child`
- (BOOL)getHandlesOfChild:(OTIOHandles *)outHandles child:(OTIOSerializableObject *)child error:(NSError **)error;

/// Returns whether a composition holds an object directly.
///
/// C: `otio_composition_has_child`
- (BOOL)getHasChild:(BOOL *)outHas child:(OTIOSerializableObject *)child error:(NSError **)error;

/// Returns where a child sits in its composition.
///
/// C: `otio_composition_index_of_child`
- (BOOL)getIndexOfChild:(NSUInteger *)outIndex child:(OTIOSerializableObject *)child error:(NSError **)error;

/// Adds a child to a composition at an index.
///
/// A negative index counts from the end, as upstream's Python does. The
/// child must not already be in a composition.
///
/// C: `otio_composition_insert_child`
- (BOOL)insertChild:(int64_t)index child:(OTIOSerializableObject *)child error:(NSError **)error;

/// Returns whether an object descends from a composition at any depth.
///
/// C: `otio_composition_is_parent_of`
- (BOOL)getIsParentOf:(BOOL *)outIs other:(OTIOSerializableObject *)other error:(NSError **)error;

/// Returns the children on either side of one, or `none` where there is
/// none.
///
/// With `OTIONeighborGapPolicyAroundTransitions`, a transition at the
/// head or tail of a track gets a gap materialized for its overhang,
/// which is why this takes a document it may edit.
///
/// C: `otio_composition_neighbors_of`
- (BOOL)getNeighborsOf:(OTIOSerializableObject *_Nullable *_Nullable)outBefore after:(OTIOSerializableObject *_Nullable *_Nullable)outAfter child:(OTIOSerializableObject *)child policy:(OTIONeighborGapPolicy)policy error:(NSError **)error;

/// Creates a bare composition: children with no layout of its own.
///
/// A nil name means none.
///
/// C: `otio_composition_new`
+ (nullable OTIOComposition *)compositionWithName:(NSString *_Nullable)name error:(NSError **)error;

/// Returns where a child sits in a composition's clock, at any depth.
///
/// C: `otio_composition_range_of_child`
- (BOOL)getRangeOfChild:(OTIOTimeRange *)outRange child:(OTIOSerializableObject *)child error:(NSError **)error;

/// Returns where the child at an index sits in its composition's clock.
///
/// A negative index counts from the end.
///
/// C: `otio_composition_range_of_child_at_index`
- (BOOL)getRangeOfChildAtIndex:(OTIOTimeRange *)outRange index:(int64_t)index error:(NSError **)error;

/// Returns where every child of a composition sits, in one pass.
///
/// `out_nodes` and `out_ranges` are filled in step, so entry `i` of one
/// goes with entry `i` of the other. Either may be nil when only the
/// count is wanted.
///
/// C: `otio_composition_ranges_of_children`
- (BOOL)getRangesOfChildren:(NSArray<OTIOSerializableObject *> *_Nullable *_Nullable)outNodes ranges:(NSArray<NSValue *> *_Nullable *_Nullable)outRanges error:(NSError **)error;

/// Removes a child by index, and returns it.
///
/// The child stays in the document with no parent.
///
/// C: `otio_composition_remove_child`
- (nullable OTIOSerializableObject *)removeChild:(int64_t)index error:(NSError **)error;

/// Clips a range to a composition's own trim.
///
/// Reports `OTIOStatusNoValue` when nothing of it is left.
///
/// C: `otio_composition_trim_child_range`
- (BOOL)getTrimChildRange:(OTIOTimeRange *)outRange childRange:(OTIOTimeRange)childRange error:(NSError **)error;

/// Returns where a child sits after the composition's trim, at any depth.
///
/// Reports `OTIOStatusNoValue` when the trim excludes it entirely.
///
/// C: `otio_composition_trimmed_range_of_child`
- (BOOL)getTrimmedRangeOfChild:(OTIOTimeRange *)outRange child:(OTIOSerializableObject *)child error:(NSError **)error;

/// Returns where the child at an index sits, after the composition's own
/// trim.
///
/// C: `otio_composition_trimmed_range_of_child_at_index`
- (BOOL)getTrimmedRangeOfChildAtIndex:(OTIOTimeRange *)outRange index:(int64_t)index error:(NSError **)error;
@end

@interface OTIOEffect (OTIOGenerated)

/// Returns an effect's own name, such as `"LinearTimeWarp"`.
///
/// C: `otio_effect_effect_name`
- (nullable NSString *)effectName:(NSError **)error;

/// Returns whether an effect is applied.
///
/// C: `otio_effect_enabled`
- (BOOL)getEnabled:(BOOL *)outEnabled error:(NSError **)error;

/// Creates an effect. `effect_name` is the effect's own name, such as
/// `"Blur"`, which is separate from the object's name.
///
/// A nil name means none.
///
/// A nil effectName means none.
///
/// C: `otio_effect_new`
+ (nullable OTIOEffect *)effectWithName:(NSString *_Nullable)name effectName:(NSString *_Nullable)effectName error:(NSError **)error;

/// Sets an effect's own name.
///
/// C: `otio_effect_set_effect_name`
- (BOOL)setEffectName:(NSString *)effectName error:(NSError **)error;

/// Sets whether an effect is applied.
///
/// C: `otio_effect_set_enabled`
- (BOOL)setEnabled:(BOOL)enabled error:(NSError **)error;

/// Sets a speed change's multiplier.
///
/// C: `otio_effect_set_time_scalar`
- (BOOL)setTimeScalar:(double)scalar error:(NSError **)error;

/// Returns a speed change's multiplier.
///
/// Only a `LinearTimeWarp` and a `FreezeFrame` have one; anything else
/// reports `OTIOStatusCoreError`.
///
/// C: `otio_effect_time_scalar`
- (BOOL)getTimeScalar:(double *)outScalar error:(NSError **)error;
@end

@interface OTIOExternalReference (OTIOGenerated)

/// Creates a media reference pointing at a URL.
///
/// A nil name means none.
///
/// A nil targetURL means none.
///
/// C: `otio_external_reference_new`
+ (nullable OTIOExternalReference *)externalReferenceWithName:(NSString *_Nullable)name targetURL:(NSString *_Nullable)targetURL error:(NSError **)error;

/// Sets where an external reference's media lives.
///
/// C: `otio_external_reference_set_target_url`
- (BOOL)setTargetURL:(NSString *)url error:(NSError **)error;

/// Returns where an external reference's media lives.
///
/// C: `otio_external_reference_target_url`
- (nullable NSString *)targetURL:(NSError **)error;
@end

@interface OTIOFreezeFrame (OTIOGenerated)

/// Creates a freeze frame: a hold on a single frame.
///
/// A nil name means none.
///
/// C: `otio_freeze_frame_new`
+ (nullable OTIOFreezeFrame *)freezeFrameWithName:(NSString *_Nullable)name error:(NSError **)error;
@end

@interface OTIOGap (OTIOGenerated)

/// Creates a gap.
///
/// A nil name means none.
///
/// C: `otio_gap_new`
+ (nullable OTIOGap *)gapWithName:(NSString *_Nullable)name error:(NSError **)error;
@end

@interface OTIOGeneratorReference (OTIOGenerated)

/// Returns which generator a generator reference names, such as
/// `"SMPTEBars"`.
///
/// C: `otio_generator_reference_kind`
- (nullable NSString *)generatorKind:(NSError **)error;

/// Creates a media reference for generated media, such as colour bars.
///
/// A nil name means none.
///
/// A nil generatorKind means none.
///
/// C: `otio_generator_reference_new`
+ (nullable OTIOGeneratorReference *)generatorReferenceWithName:(NSString *_Nullable)name generatorKind:(NSString *_Nullable)generatorKind error:(NSError **)error;

/// Sets which generator a generator reference names.
///
/// C: `otio_generator_reference_set_kind`
- (BOOL)setGeneratorKind:(NSString *)kind error:(NSError **)error;
@end

@interface OTIOImageSequenceReference (OTIOGenerated)

/// Returns the part of each frame's filename before the frame number.
///
/// C: `otio_image_sequence_reference_name_prefix`
- (nullable NSString *)namePrefix:(NSError **)error;

/// Returns the part of each frame's filename after the frame number.
///
/// C: `otio_image_sequence_reference_name_suffix`
- (nullable NSString *)nameSuffix:(NSError **)error;

/// Creates a media reference for a numbered sequence of image files.
///
/// The filename parts and the numbers start empty and at zero; set them
/// with `setNumbers` and the calls beside it.
///
/// A nil name means none.
///
/// C: `otio_image_sequence_reference_new`
+ (nullable OTIOImageSequenceReference *)imageSequenceReferenceWithName:(NSString *_Nullable)name error:(NSError **)error;

/// Returns the numbers describing how an image sequence is laid out.
///
/// C: `otio_image_sequence_reference_numbers`
- (BOOL)getNumbers:(OTIOImageSequence *)outNumbers error:(NSError **)error;

/// Sets the part of each frame's filename before the frame number.
///
/// C: `otio_image_sequence_reference_set_name_prefix`
- (BOOL)setNamePrefix:(NSString *)prefix error:(NSError **)error;

/// Sets the part of each frame's filename after the frame number.
///
/// C: `otio_image_sequence_reference_set_name_suffix`
- (BOOL)setNameSuffix:(NSString *)suffix error:(NSError **)error;

/// Sets the numbers describing how an image sequence is laid out.
///
/// C: `otio_image_sequence_reference_set_numbers`
- (BOOL)setNumbers:(OTIOImageSequence)numbers error:(NSError **)error;

/// Sets the directory an image sequence's frames sit in.
///
/// C: `otio_image_sequence_reference_set_target_url_base`
- (BOOL)setTargetURLBase:(NSString *)urlBase error:(NSError **)error;

/// Returns the directory an image sequence's frames sit in.
///
/// C: `otio_image_sequence_reference_target_url_base`
- (nullable NSString *)targetURLBase:(NSError **)error;
@end

@interface OTIOItem (OTIOGenerated)

/// Adds an effect to an item. Effects apply in the order they are added.
///
/// C: `otio_item_append_effect`
- (BOOL)appendEffect:(OTIOSerializableObject *)effectHandle error:(NSError **)error;

/// Adds a marker to an item.
///
/// C: `otio_item_append_marker`
- (BOOL)appendMarker:(OTIOSerializableObject *)markerHandle error:(NSError **)error;

/// Returns the span of media an object could draw on, before trimming.
///
/// C: `otio_item_available_range`
- (BOOL)getAvailableRange:(OTIOTimeRange *)outRange error:(NSError **)error;

/// Clears an item's display tint.
///
/// C: `otio_item_clear_color`
- (BOOL)clearColor:(NSError **)error;

/// Clears an item's source range, so that it takes all of its media.
///
/// C: `otio_item_clear_source_range`
- (BOOL)clearSourceRange:(NSError **)error;

/// Returns an item's display tint, and the name that goes with it.
///
/// `out_name` may be nil if the name is not wanted. Reports
/// `OTIOStatusNoValue` for an untinted item.
///
/// C: `otio_item_color`
- (BOOL)getColor:(OTIOColor *)outColor name:(NSString *_Nullable *_Nullable)outName error:(NSError **)error;

/// Returns how long an object occupies its parent's timeline.
///
/// C: `otio_item_duration`
- (BOOL)getDuration:(OTIORationalTime *)outDuration error:(NSError **)error;

/// Returns one of an item's effects.
///
/// C: `otio_item_effect_at`
- (nullable OTIOSerializableObject *)effectAt:(NSUInteger)index error:(NSError **)error;

/// Returns how many effects an item carries.
///
/// C: `otio_item_effect_count`
- (BOOL)getEffectCount:(NSUInteger *)outCount error:(NSError **)error;

/// Returns whether an item contributes to its composition.
///
/// C: `otio_item_enabled`
- (BOOL)getEnabled:(BOOL *)outEnabled error:(NSError **)error;

/// Returns one of an item's markers.
///
/// C: `otio_item_marker_at`
- (nullable OTIOSerializableObject *)markerAt:(NSUInteger)index error:(NSError **)error;

/// Returns how many markers an item carries.
///
/// C: `otio_item_marker_count`
- (BOOL)getMarkerCount:(NSUInteger *)outCount error:(NSError **)error;

/// Creates a bare item: something that occupies time without saying what
/// fills it.
///
/// A nil name means none.
///
/// C: `otio_item_new`
+ (nullable OTIOItem *)itemWithName:(NSString *_Nullable)name error:(NSError **)error;

/// Returns where an object sits in its parent's clock.
///
/// C: `otio_item_range_in_parent`
- (BOOL)getRangeInParent:(OTIOTimeRange *)outRange error:(NSError **)error;

/// Removes one of an item's effects, and returns it.
///
/// C: `otio_item_remove_effect`
- (nullable OTIOSerializableObject *)removeEffect:(NSUInteger)index error:(NSError **)error;

/// Removes one of an item's markers, and returns it.
///
/// The marker stays in the document; remove it with
/// `removeFromTimelineRecursive` if nothing else holds it.
///
/// C: `otio_item_remove_marker`
- (nullable OTIOSerializableObject *)removeMarker:(NSUInteger)index error:(NSError **)error;

/// Sets an item's display tint. `name` may be nil for an unnamed colour.
///
/// C: `otio_item_set_color`
- (BOOL)setColor:(OTIOColor)color name:(NSString *_Nullable)name error:(NSError **)error;

/// Sets whether an item contributes to its composition.
///
/// C: `otio_item_set_enabled`
- (BOOL)setEnabled:(BOOL)enabled error:(NSError **)error;

/// Sets the portion of its media an item uses.
///
/// C: `otio_item_set_source_range`
- (BOOL)setSourceRange:(OTIOTimeRange)range error:(NSError **)error;

/// Returns the portion of its media an item uses.
///
/// Reports `OTIOStatusNoValue` for an item that takes all of it.
///
/// C: `otio_item_source_range`
- (BOOL)getSourceRange:(OTIOTimeRange *)outRange error:(NSError **)error;

/// Returns the span of media an object uses, in its own clock.
///
/// C: `otio_item_trimmed_range`
- (BOOL)getTrimmedRange:(OTIOTimeRange *)outRange error:(NSError **)error;

/// Returns where an object sits in its parent's clock, after the parent's
/// own trim.
///
/// Reports `OTIOStatusNoValue` when the parent's trim excludes the object
/// entirely.
///
/// C: `otio_item_trimmed_range_in_parent`
- (BOOL)getTrimmedRangeInParent:(OTIOTimeRange *)outRange error:(NSError **)error;

/// Returns the span of media an object shows, including what its
/// transitions reach into.
///
/// C: `otio_item_visible_range`
- (BOOL)getVisibleRange:(OTIOTimeRange *)outRange error:(NSError **)error;
@end

@interface OTIOLinearTimeWarp (OTIOGenerated)

/// Creates a constant-rate speed change. A `time_scalar` of 2.0 plays
/// twice as fast.
///
/// A nil name means none.
///
/// C: `otio_linear_time_warp_new`
+ (nullable OTIOLinearTimeWarp *)linearTimeWarpWithName:(NSString *_Nullable)name timeScalar:(double)timeScalar error:(NSError **)error;
@end

@interface OTIOMarker (OTIOGenerated)

/// Returns a marker's tint, and the name that goes with it.
///
/// `out_name` may be nil if the name is not wanted. Reports
/// `OTIOStatusNoValue` for an untinted marker.
///
/// C: `otio_marker_color`
- (BOOL)getColor:(OTIOColor *)outColor name:(NSString *_Nullable *_Nullable)outName error:(NSError **)error;

/// Returns a marker's note.
///
/// C: `otio_marker_comment`
- (nullable NSString *)comment:(NSError **)error;

/// Returns the span a marker covers.
///
/// C: `otio_marker_marked_range`
- (BOOL)getMarkedRange:(OTIOTimeRange *)outRange error:(NSError **)error;

/// Creates a marker covering `marked_range`.
///
/// A nil name means none.
///
/// C: `otio_marker_new`
+ (nullable OTIOMarker *)markerWithName:(NSString *_Nullable)name markedRange:(OTIOTimeRange)markedRange error:(NSError **)error;

/// Sets a marker's tint. `name` may be nil for an unnamed colour.
///
/// C: `otio_marker_set_color`
- (BOOL)setColor:(OTIOColor)color name:(NSString *_Nullable)name error:(NSError **)error;

/// Sets a marker's note.
///
/// C: `otio_marker_set_comment`
- (BOOL)setComment:(NSString *)comment error:(NSError **)error;

/// Sets the span a marker covers.
///
/// C: `otio_marker_set_marked_range`
- (BOOL)setMarkedRange:(OTIOTimeRange)range error:(NSError **)error;
@end

@interface OTIOMediaReference (OTIOGenerated)

/// Returns the image bounds a reference says its media has.
///
/// Where there is nothing to report this fails with OTIOStatusNoValue,
/// which OTIOIsNoValue tells apart from a real failure.
///
/// C: `otio_media_reference_available_image_bounds`
- (BOOL)getAvailableImageBounds:(OTIOBox2d *)outBounds error:(NSError **)error;

/// Returns the span of media a reference says is available.
///
/// Reports `OTIOStatusNoValue` for a reference that does not say.
///
/// C: `otio_media_reference_available_range`
- (BOOL)getAvailableRange:(OTIOTimeRange *)outRange error:(NSError **)error;

/// Clears the image bounds a reference says its media has.
///
/// C: `otio_media_reference_clear_available_image_bounds`
- (BOOL)clearAvailableImageBounds:(NSError **)error;

/// Clears the span of media a reference says is available.
///
/// C: `otio_media_reference_clear_available_range`
- (BOOL)clearAvailableRange:(NSError **)error;

/// Sets the image bounds a reference says its media has.
///
/// C: `otio_media_reference_set_available_image_bounds`
- (BOOL)setAvailableImageBounds:(OTIOBox2d)bounds error:(NSError **)error;

/// Sets the span of media a reference says is available.
///
/// C: `otio_media_reference_set_available_range`
- (BOOL)setAvailableRange:(OTIOTimeRange)range error:(NSError **)error;
@end

@interface OTIOMissingReference (OTIOGenerated)

/// Creates a media reference for media known to exist somewhere unknown.
///
/// A nil name means none.
///
/// C: `otio_missing_reference_new`
+ (nullable OTIOMissingReference *)missingReferenceWithName:(NSString *_Nullable)name error:(NSError **)error;
@end

@interface OTIOSerializableObject (OTIOGenerated)

/// Returns one of an object's children.
///
/// C: `otio_node_child_at`
- (nullable OTIOSerializableObject *)childAt:(NSUInteger)index error:(NSError **)error;

/// Returns how many children an object holds.
///
/// Tracks, stacks, bare compositions and serializable collections hold
/// children; anything else reports `OTIOStatusCoreError`.
///
/// C: `otio_node_child_count`
- (BOOL)getChildCount:(NSUInteger *)outCount error:(NSError **)error;

/// Returns an object's children.
///
/// C: `otio_node_children`
- (nullable NSArray<OTIOSerializableObject *> *)children:(NSError **)error;

/// Returns whether two handles name the same object.
///
/// This is object identity: two handles are equal only if they were
/// issued for the same occupant of the same slot.
///
/// C: `otio_node_equal`
- (BOOL)equals:(OTIOSerializableObject *)right;

/// Returns every clip at or below an object, in order.
///
/// C: `otio_node_find_clips`
- (nullable NSArray<OTIOSerializableObject *> *)findClips:(NSError **)error;

/// Returns the outermost object above this one.
///
/// C: `otio_node_highest_ancestor`
- (nullable OTIOSerializableObject *)highestAncestor:(NSError **)error;

/// Returns whether a handle names no object.
///
/// C: `otio_node_is_none`
- (BOOL)isNone;

/// Returns what kind of object a handle names.
///
/// C: `otio_node_kind`
- (BOOL)getSchemaKind:(OTIONodeKind *)outKind error:(NSError **)error;

/// Returns an object's name, which is empty for an object that has none.
///
/// C: `otio_node_name`
- (nullable NSString *)name:(NSError **)error;

/// Returns the handle that names no object.
///
/// Fields that may be absent, such as a timeline's tracks before one is
/// set, report this. Compare against it with `isNone`.
///
/// C: `otio_node_none`
+ (OTIOSerializableObject *)none;

/// Returns whether an object sits over its neighbours rather than beside
/// them.
///
/// Only a transition does, which is why one does not advance the playhead
/// when a track is laid out.
///
/// C: `otio_node_overlapping`
- (BOOL)getOverlapping:(BOOL *)outOverlapping error:(NSError **)error;

/// Returns the composition an object sits in.
///
/// Reports `OTIOStatusNoValue` for an object that is in none, which is
/// what a freshly created one is.
///
/// C: `otio_node_parent`
- (nullable OTIOSerializableObject *)parent:(NSError **)error;

/// Returns the schema name an object serializes as, such as `"Clip"`.
///
/// C: `otio_node_schema_name`
- (nullable NSString *)schemaName:(NSError **)error;

/// Returns the schema version an object serializes as.
///
/// C: `otio_node_schema_version`
- (BOOL)getSchemaVersion:(uint32_t *)outVersion error:(NSError **)error;

/// Sets an object's name.
///
/// C: `otio_node_set_name`
- (BOOL)setName:(NSString *)name error:(NSError **)error;

/// Writes one object of a document as OTIO JSON.
///
/// Upstream's `write_to_string` takes any object, not only a timeline, so
/// this does too.
///
/// C: `otio_node_to_json`
- (nullable NSString *)toJSON:(NSUInteger)indent error:(NSError **)error;

/// Restates an instant from one object's clock in another's.
///
/// C: `otio_node_transformed_time`
- (BOOL)getTransformedTime:(OTIORationalTime *)outTime time:(OTIORationalTime)time to:(OTIOSerializableObject *)to error:(NSError **)error;

/// Restates a span from one object's clock in another's.
///
/// C: `otio_node_transformed_time_range`
- (BOOL)getTransformedTimeRange:(OTIOTimeRange *)outRange range:(OTIOTimeRange)range to:(OTIOSerializableObject *)to error:(NSError **)error;

/// Returns whether an object covers what is beneath it when its
/// composition is flattened.
///
/// C: `otio_node_visible`
- (BOOL)getVisible:(BOOL *)outVisible error:(NSError **)error;

/// Returns whether a handle still names a live object.
///
/// C: `otio_document_contains`
- (BOOL)isLive;

/// Copies an object and everything it owns, into the same document.
///
/// The copy has no parent, whatever the original had.
///
/// C: `otio_document_deep_clone`
- (nullable OTIOSerializableObject *)deepClone:(NSError **)error;

/// Removes one object from the document.
///
/// Anything that referred to it still holds a handle, and that handle is
/// now stale: a lookup fails rather than reaching whatever takes the slot
/// next. To remove an object together with everything hanging off it, use
/// `removeFromTimelineRecursive`.
///
/// C: `otio_document_remove`
- (BOOL)removeFromTimeline:(NSError **)error;

/// Removes an object and everything it owns: children, markers, effects
/// and media references.
///
/// C: `otio_document_remove_recursive`
- (BOOL)removeFromTimelineRecursive:(NSError **)error;
@end

@interface OTIOSerializableCollection (OTIOGenerated)

/// Creates a serializable collection: a group of objects with no timing.
///
/// A nil name means none.
///
/// C: `otio_serializable_collection_new`
+ (nullable OTIOSerializableCollection *)serializableCollectionWithName:(NSString *_Nullable)name error:(NSError **)error;
@end

@interface OTIOStack (OTIOGenerated)

/// Creates a stack.
///
/// A nil name means none.
///
/// C: `otio_stack_new`
+ (nullable OTIOStack *)stackWithName:(NSString *_Nullable)name error:(NSError **)error;
@end

@interface OTIOTimeEffect (OTIOGenerated)

/// Creates a time effect: an effect that alters timing and has no
/// parameters.
///
/// A nil name means none.
///
/// A nil effectName means none.
///
/// C: `otio_time_effect_new`
+ (nullable OTIOTimeEffect *)timeEffectWithName:(NSString *_Nullable)name effectName:(NSString *_Nullable)effectName error:(NSError **)error;
@end

@interface OTIOTimeline (OTIOGenerated)

/// Clears where a timeline begins.
///
/// C: `otio_timeline_clear_global_start_time`
- (BOOL)clearGlobalStartTime:(NSError **)error;

/// Returns where a timeline begins, such as `01:00:00:00`.
///
/// Reports `OTIOStatusNoValue` for a timeline that does not say.
///
/// C: `otio_timeline_global_start_time`
- (BOOL)getGlobalStartTime:(OTIORationalTime *)outTime error:(NSError **)error;

/// Creates a timeline, with an empty stack named `"tracks"` already in
/// it.
///
/// Upstream's `Timeline()` builds that stack in its constructor, and its
/// own tests append to a fresh timeline's tracks without making one
/// first, so a timeline from here arrives the same way rather than
/// leaving every binding to invent the difference. Replace it with
/// `setTracks` to use a stack of your own; the one built here is thrown
/// away with the document.
///
/// A nil name means none.
///
/// C: `otio_timeline_new`
+ (nullable OTIOTimeline *)timelineWithName:(NSString *_Nullable)name error:(NSError **)error;

/// Sets where a timeline begins.
///
/// C: `otio_timeline_set_global_start_time`
- (BOOL)setGlobalStartTime:(OTIORationalTime)time error:(NSError **)error;

/// Sets the stack holding a timeline's tracks.
///
/// The stack's parent is set to the timeline, as upstream's does.
///
/// Passing `none` puts a fresh empty stack there rather than nothing,
/// because that is what upstream's setter does: its own
/// `test_timeline.py` sets `tracks` to `None` and then asserts that
/// `tl.tracks` is still a `Stack`. A timeline with no tracks at all is
/// not a thing a caller can reach through upstream's API, so it is not
/// one they can reach through this one.
///
/// Whatever stack was there is not destroyed. It stays in the document,
/// parentless, so it can be put somewhere else; dropping it is a separate
/// `removeFromTimeline` call. That is the same bargain `detachChild`
/// makes, and leaving its parent pointing at the timeline instead would
/// mean an object claiming a parent that has disowned it. A displaced
/// stack that some other timeline has since taken as its own keeps that
/// timeline as its parent, since this one is not the timeline disowning
/// it.
///
/// A nil tracks means none.
///
/// C: `otio_timeline_set_tracks`
- (BOOL)setTracks:(OTIOSerializableObject *_Nullable)tracks error:(NSError **)error;

/// Returns the stack holding a timeline's tracks.
///
/// Reports `OTIOStatusNoValue` for a timeline that has none.
///
/// C: `otio_timeline_tracks`
- (nullable OTIOSerializableObject *)tracks:(NSError **)error;
@end

@interface OTIOTrack (OTIOGenerated)

/// Returns what a track carries, such as `"Video"` or `"Audio"`.
///
/// C: `otio_track_kind`
- (nullable NSString *)kind:(NSError **)error;

/// Creates a track. `kind` may be nil, which means `"Video"`, as
/// upstream's default does.
///
/// C: `otio_track_new`
+ (nullable OTIOTrack *)trackWithName:(NSString *_Nullable)name kind:(NSString *_Nullable)kind error:(NSError **)error;

/// Sets what a track carries.
///
/// C: `otio_track_set_kind`
- (BOOL)setKind:(NSString *)kind error:(NSError **)error;
@end

@interface OTIOTransition (OTIOGenerated)

/// Returns whether a transition is applied.
///
/// C: `otio_transition_enabled`
- (BOOL)getEnabled:(BOOL *)outEnabled error:(NSError **)error;

/// Returns how far a transition reaches into the item before it.
///
/// C: `otio_transition_in_offset`
- (BOOL)getInOffset:(OTIORationalTime *)outOffset error:(NSError **)error;

/// Creates a transition. Its offsets start at zero.
///
/// A nil name means none.
///
/// A nil transitionType means none.
///
/// C: `otio_transition_new`
+ (nullable OTIOTransition *)transitionWithName:(NSString *_Nullable)name transitionType:(NSString *_Nullable)transitionType error:(NSError **)error;

/// Returns how far a transition reaches into the item after it.
///
/// C: `otio_transition_out_offset`
- (BOOL)getOutOffset:(OTIORationalTime *)outOffset error:(NSError **)error;

/// Sets whether a transition is applied.
///
/// C: `otio_transition_set_enabled`
- (BOOL)setEnabled:(BOOL)enabled error:(NSError **)error;

/// Sets how far a transition reaches into the item before it.
///
/// C: `otio_transition_set_in_offset`
- (BOOL)setInOffset:(OTIORationalTime)offset error:(NSError **)error;

/// Sets how far a transition reaches into the item after it.
///
/// C: `otio_transition_set_out_offset`
- (BOOL)setOutOffset:(OTIORationalTime)offset error:(NSError **)error;

/// Sets the kind of transition.
///
/// C: `otio_transition_set_type`
- (BOOL)setType:(NSString *)transitionType error:(NSError **)error;

/// Returns the kind of transition, such as `"SMPTE_Dissolve"`.
///
/// C: `otio_transition_type`
- (nullable NSString *)type:(NSError **)error;
@end

#pragma mark - Metadata

/// The free-form dictionary every named object carries.
@interface OTIOMetadata : NSObject {
@private
    OTIOSerializableObject *_object;
}

/// The object whose metadata this is.
@property (nonatomic, readonly, strong) OTIOSerializableObject *object;

/// Makes one for an object. Reach for -[OTIOSerializableObject metadata] instead.
- (instancetype)initWithObject:(OTIOSerializableObject *)object;
- (instancetype)init NS_UNAVAILABLE;

/// Empties an object's metadata.
///
/// C: `otio_metadata_clear`
- (BOOL)clear:(NSError **)error;

/// Returns whether anything sits at a path.
///
/// C: `otio_metadata_contains`
- (BOOL)getContains:(BOOL *)outContains path:(NSString *)path error:(NSError **)error;

/// Reads a boolean from the metadata.
///
/// C: `otio_metadata_get_bool`
- (BOOL)getBool:(BOOL *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads a rectangle from the metadata.
///
/// C: `otio_metadata_get_box2d`
- (BOOL)getBox2d:(OTIOBox2d *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads a colour, and the name that goes with it, from the metadata.
///
/// `out_name` may be nil if the name is not wanted.
///
/// C: `otio_metadata_get_color`
- (BOOL)getColor:(OTIOColor *)outValue name:(NSString *_Nullable *_Nullable)outName path:(NSString *)path error:(NSError **)error;

/// Reads a number from the metadata.
///
/// C: `otio_metadata_get_double`
- (BOOL)getDouble:(double *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads a signed integer from the metadata.
///
/// C: `otio_metadata_get_int`
- (BOOL)getInt:(int64_t *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads a handle to an OTIO object held in the metadata.
///
/// C: `otio_metadata_get_object`
- (nullable OTIOSerializableObject *)getObject:(NSString *)path error:(NSError **)error;

/// Reads a time from the metadata.
///
/// C: `otio_metadata_get_rational_time`
- (BOOL)getRationalTime:(OTIORationalTime *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads a string from the metadata.
///
/// C: `otio_metadata_get_string`
- (nullable NSString *)getString:(NSString *)path error:(NSError **)error;

/// Reads a span from the metadata.
///
/// C: `otio_metadata_get_time_range`
- (BOOL)getTimeRange:(OTIOTimeRange *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads a transform from the metadata.
///
/// C: `otio_metadata_get_time_transform`
- (BOOL)getTimeTransform:(OTIOTimeTransform *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads an unsigned integer from the metadata.
///
/// C: `otio_metadata_get_uint`
- (BOOL)getUint:(uint64_t *)outValue path:(NSString *)path error:(NSError **)error;

/// Reads a point from the metadata.
///
/// C: `otio_metadata_get_v2d`
- (BOOL)getV2d:(OTIOV2d *)outValue path:(NSString *)path error:(NSError **)error;

/// Returns the key at an index of a dictionary at a path.
///
/// Keys are ordered, so walking the indices walks them the same way
/// twice.
///
/// Where there is nothing to report this fails with OTIOStatusNoValue,
/// which OTIOIsNoValue tells apart from a real failure.
///
/// C: `otio_metadata_key_at`
- (nullable NSString *)keyAt:(NSString *)path index:(NSUInteger)index error:(NSError **)error;

/// Returns what kind of value sits at a path.
///
/// An empty or nil path names the metadata dictionary itself, which is
/// always `OTIOValueKindDictionary`.
///
/// Where there is nothing to report this fails with OTIOStatusNoValue,
/// which OTIOIsNoValue tells apart from a real failure.
///
/// C: `otio_metadata_kind`
- (BOOL)getKind:(OTIOValueKind *)outKind path:(NSString *)path error:(NSError **)error;

/// Returns how many entries a dictionary or an array at a path holds.
///
/// Where there is nothing to report this fails with OTIOStatusNoValue,
/// which OTIOIsNoValue tells apart from a real failure.
///
/// C: `otio_metadata_len`
- (BOOL)getLen:(NSUInteger *)outLen path:(NSString *)path error:(NSError **)error;

/// Removes whatever sits at a path.
///
/// Removing a key of a dictionary takes the key with it; removing an
/// element of an array shortens the array.
///
/// Where there is nothing to report this fails with OTIOStatusNoValue,
/// which OTIOIsNoValue tells apart from a real failure.
///
/// C: `otio_metadata_remove`
- (BOOL)remove:(NSString *)path error:(NSError **)error;

/// Writes a boolean into the metadata.
///
/// C: `otio_metadata_set_bool`
- (BOOL)setBool:(NSString *)path value:(BOOL)value error:(NSError **)error;

/// Writes a rectangle into the metadata.
///
/// C: `otio_metadata_set_box2d`
- (BOOL)setBox2d:(NSString *)path value:(OTIOBox2d)value error:(NSError **)error;

/// Writes a colour into the metadata. `name` may be nil for an unnamed
/// one.
///
/// C: `otio_metadata_set_color`
- (BOOL)setColor:(NSString *)path value:(OTIOColor)value name:(NSString *_Nullable)name error:(NSError **)error;

/// Writes an empty dictionary into the metadata, to be filled through
/// deeper paths.
///
/// C: `otio_metadata_set_dictionary`
- (BOOL)setDictionary:(NSString *)path error:(NSError **)error;

/// Writes a number into the metadata.
///
/// C: `otio_metadata_set_double`
- (BOOL)setDouble:(NSString *)path value:(double)value error:(NSError **)error;

/// Writes a signed integer into the metadata.
///
/// C: `otio_metadata_set_int`
- (BOOL)setInt:(NSString *)path value:(int64_t)value error:(NSError **)error;

/// Writes a nil into the metadata.
///
/// C: `otio_metadata_set_null`
- (BOOL)setNull:(NSString *)path error:(NSError **)error;

/// Writes a handle to an OTIO object into the metadata.
///
/// The object stays where it is and the metadata names it. Nothing checks
/// that the handle is live, because a document being built may not hold
/// the object yet.
///
/// C: `otio_metadata_set_object`
- (BOOL)setObject:(NSString *)path value:(OTIOSerializableObject *)value error:(NSError **)error;

/// Writes a time into the metadata.
///
/// C: `otio_metadata_set_rational_time`
- (BOOL)setRationalTime:(NSString *)path value:(OTIORationalTime)value error:(NSError **)error;

/// Writes a string into the metadata.
///
/// C: `otio_metadata_set_string`
- (BOOL)setString:(NSString *)path value:(NSString *)value error:(NSError **)error;

/// Writes a span into the metadata.
///
/// C: `otio_metadata_set_time_range`
- (BOOL)setTimeRange:(NSString *)path value:(OTIOTimeRange)value error:(NSError **)error;

/// Writes a transform into the metadata.
///
/// C: `otio_metadata_set_time_transform`
- (BOOL)setTimeTransform:(NSString *)path value:(OTIOTimeTransform)value error:(NSError **)error;

/// Writes an unsigned integer into the metadata.
///
/// C: `otio_metadata_set_uint`
- (BOOL)setUint:(NSString *)path value:(uint64_t)value error:(NSError **)error;

/// Writes a point into the metadata.
///
/// C: `otio_metadata_set_v2d`
- (BOOL)setV2d:(NSString *)path value:(OTIOV2d)value error:(NSError **)error;

/// Writes an array of `len` nulls into the metadata, to be filled by
/// index.
///
/// C: `otio_metadata_set_vector`
- (BOOL)setVector:(NSString *)path len:(NSUInteger)len error:(NSError **)error;
@end

@interface OTIOSerializableObjectWithMetadata (OTIOMetadata)

/// The object's metadata, which is a dictionary of its own.
///
/// A path names a value inside it, a step at a time, separated by dots:
/// `cmx_3600.reel` reaches the reel of the dictionary the EDL adapter left
/// behind, and `takes[0]` the first entry of a list.
///
/// A path is followed, not created. Writing one step deep always works, but
/// a deeper one needs its dictionary to exist first.
@property (nonatomic, readonly, strong) OTIOMetadata *metadata;

@end

NS_ASSUME_NONNULL_END
