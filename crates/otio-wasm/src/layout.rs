// Generated by `cargo run -p otio-sdk-gen`.
// Do not edit: run the generator instead. The signatures and the prose
// below are read out of `crates/otio-capi/src`, so this file is that
// crate restated for TypeScript rather than a second description of it.

//! Proof that the byte offsets the TypeScript uses are the ones Rust
//! lays these structs out at.
//!
//! The generator works the offsets out itself, from `wasm32`'s rules rather
//! than the host's, because a pointer and a `usize` are four bytes there and
//! eight here. Getting one wrong would not fail anywhere: the TypeScript
//! would read the wrong part of the right struct and hand back a plausible
//! number. So the generator also writes these, and a wrong offset stops the
//! build.

#![cfg(target_arch = "wasm32")]

use core::mem::{align_of, offset_of, size_of};

use otio::{
    OtioNode,
    OtioRationalTime,
    OtioTimeRange,
    OtioTimeTransform,
    OtioColor,
    OtioV2d,
    OtioBox2d,
    OtioImageSequence,
    OtioHandles,
    OtioReadOptions,
    OtioWriteOptions,
};

const _: () = {
    assert!(size_of::<OtioNode>() == 8);
    assert!(align_of::<OtioNode>() == 4);
    assert!(offset_of!(OtioNode, index) == 0);
    assert!(offset_of!(OtioNode, generation) == 4);

    assert!(size_of::<OtioRationalTime>() == 16);
    assert!(align_of::<OtioRationalTime>() == 8);
    assert!(offset_of!(OtioRationalTime, value) == 0);
    assert!(offset_of!(OtioRationalTime, rate) == 8);

    assert!(size_of::<OtioTimeRange>() == 32);
    assert!(align_of::<OtioTimeRange>() == 8);
    assert!(offset_of!(OtioTimeRange, start_time) == 0);
    assert!(offset_of!(OtioTimeRange, duration) == 16);

    assert!(size_of::<OtioTimeTransform>() == 32);
    assert!(align_of::<OtioTimeTransform>() == 8);
    assert!(offset_of!(OtioTimeTransform, offset) == 0);
    assert!(offset_of!(OtioTimeTransform, scale) == 16);
    assert!(offset_of!(OtioTimeTransform, rate) == 24);

    assert!(size_of::<OtioColor>() == 32);
    assert!(align_of::<OtioColor>() == 8);
    assert!(offset_of!(OtioColor, r) == 0);
    assert!(offset_of!(OtioColor, g) == 8);
    assert!(offset_of!(OtioColor, b) == 16);
    assert!(offset_of!(OtioColor, a) == 24);

    assert!(size_of::<OtioV2d>() == 16);
    assert!(align_of::<OtioV2d>() == 8);
    assert!(offset_of!(OtioV2d, x) == 0);
    assert!(offset_of!(OtioV2d, y) == 8);

    assert!(size_of::<OtioBox2d>() == 32);
    assert!(align_of::<OtioBox2d>() == 8);
    assert!(offset_of!(OtioBox2d, min) == 0);
    assert!(offset_of!(OtioBox2d, max) == 16);

    assert!(size_of::<OtioImageSequence>() == 40);
    assert!(align_of::<OtioImageSequence>() == 8);
    assert!(offset_of!(OtioImageSequence, start_frame) == 0);
    assert!(offset_of!(OtioImageSequence, frame_step) == 8);
    assert!(offset_of!(OtioImageSequence, rate) == 16);
    assert!(offset_of!(OtioImageSequence, frame_zero_padding) == 24);
    assert!(offset_of!(OtioImageSequence, missing_frame_policy) == 32);

    assert!(size_of::<OtioHandles>() == 48);
    assert!(align_of::<OtioHandles>() == 8);
    assert!(offset_of!(OtioHandles, has_before) == 0);
    assert!(offset_of!(OtioHandles, before) == 8);
    assert!(offset_of!(OtioHandles, has_after) == 24);
    assert!(offset_of!(OtioHandles, after) == 32);

    assert!(size_of::<OtioReadOptions>() == 16);
    assert!(align_of::<OtioReadOptions>() == 8);
    assert!(offset_of!(OtioReadOptions, rate) == 0);
    assert!(offset_of!(OtioReadOptions, name_column) == 8);
    assert!(offset_of!(OtioReadOptions, ignore_timecode_mismatch) == 12);
    assert!(offset_of!(OtioReadOptions, aaf_keep_nesting) == 13);
    assert!(offset_of!(OtioReadOptions, aaf_markers_on_slots) == 14);
    assert!(offset_of!(OtioReadOptions, aaf_bake_keyframes) == 15);

    assert!(size_of::<OtioWriteOptions>() == 48);
    assert!(align_of::<OtioWriteOptions>() == 8);
    assert!(offset_of!(OtioWriteOptions, rate) == 0);
    assert!(offset_of!(OtioWriteOptions, edl_style) == 8);
    assert!(offset_of!(OtioWriteOptions, reelname_len) == 12);
    assert!(offset_of!(OtioWriteOptions, video_format) == 16);
    assert!(offset_of!(OtioWriteOptions, aaf_prefer_file_mob_id) == 20);
    assert!(offset_of!(OtioWriteOptions, aaf_use_empty_mob_ids) == 21);
    assert!(offset_of!(OtioWriteOptions, aaf_embed_essence) == 22);
    assert!(offset_of!(OtioWriteOptions, aaf_create_edgecode) == 23);
    assert!(offset_of!(OtioWriteOptions, aaf_user) == 24);
    assert!(offset_of!(OtioWriteOptions, aaf_time) == 32);
    assert!(offset_of!(OtioWriteOptions, aaf_id_seed) == 40);

};
