// Generated by `cargo run -p otio-wasm --bin otio-ts-gen`.
// Do not edit: run the generator instead. The signatures and the prose
// below are read out of `crates/otio-capi/src`, so this file is that
// crate restated for TypeScript rather than a second description of it.

/**
 * One function per C entry point, doing the marshalling and nothing else.
 *
 * These are the whole C ABI, faithfully: nothing is left out and nothing
 * is renamed beyond dropping the `otio_` prefix. What they do not do is
 * read like TypeScript — a document is a number here, and an object is a
 * handle. The classes in `api.ts` are the surface; this is what they call.
 *
 * Three things do happen here, because they are mechanical and nobody
 * should have to remember them: a failing status becomes a thrown
 * `OtioError`, `OTIO_STATUS_NO_VALUE` becomes `undefined`, and every
 * buffer the library hands back is read and freed before returning.
 */

import { check, exports, openStack, readBuffer, readCString } from "../runtime.js";
import * as types from "./types.js";
import * as values from "./values.js";

/* The status that means "the answer is nothing", which is not a failure. */
const NO_VALUE = 3;

/**
 * Collapses a stack's tracks into one, top layer winning where it is visible.
 */
export function algorithmFlattenStack(document: number, stack: types.NodeHandle): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(stack);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_algorithm_flatten_stack(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Collapses a list of tracks into one, lowest first.
 */
export function algorithmFlattenTracks(document: number, tracks: readonly types.NodeHandle[]): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = $stack.nodes(tracks);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_algorithm_flatten_tracks(document, $arg0.pointer, $arg0.length, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns a copy of a track holding only what falls inside a span.
 *
 * The copy is added to the same document and has no parent.
 */
export function algorithmTrackTrimmedToRange(document: number, track: types.NodeHandle, trimRange: values.TimeRangeLike): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(track);
    const $arg1 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, trimRange);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_algorithm_track_trimmed_to_range(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns which of a clip's media references is in use.
 */
export function clipActiveMediaReferenceKey(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_clip_active_media_reference_key(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns one of a clip's media references.
 *
 * A null `key` means the active one. Answers `undefined` if the
 * key names nothing, which for the active key is a clip with no media.
 */
export function clipMediaReference(document: number, node: types.NodeHandle, key: string | undefined): types.NodeHandle | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = key === undefined ? 0 : $stack.text(key);
    const $out0 = $stack.alloc(8, 4);
    const $status = exports().otio_clip_media_reference(document, $receiver, $arg0, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns how many media references a clip holds.
 */
export function clipMediaReferenceCount(document: number, node: types.NodeHandle): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_clip_media_reference_count(document, $receiver, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the key of one of a clip's media references.
 *
 * The references are ordered by key, so walking the indices walks them in a
 * stable order.
 */
export function clipMediaReferenceKeyAt(document: number, node: types.NodeHandle, index: number): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = index;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_clip_media_reference_key_at(document, $receiver, $arg0, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Creates a clip. `name` may be null for an unnamed one.
 */
export function clipNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_clip_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Removes one of a clip's media references, and returns it.
 */
export function clipRemoveMediaReference(document: number, node: types.NodeHandle, key: string): types.NodeHandle | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(key);
    const $out0 = $stack.alloc(8, 4);
    const $status = exports().otio_clip_remove_media_reference(document, $receiver, $arg0, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets which of a clip's media references is in use.
 */
export function clipSetActiveMediaReferenceKey(document: number, node: types.NodeHandle, key: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(key);
    check(exports().otio_clip_set_active_media_reference_key(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets one of a clip's media references, adding it if the key is new.
 */
export function clipSetMediaReference(document: number, node: types.NodeHandle, key: string, reference: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(key);
    const $arg1 = $stack.node(reference);
    check(exports().otio_clip_set_media_reference(document, $receiver, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Creates a composable: something that sits in a composition and nothing
 * more.
 */
export function composableNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_composable_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Adds a child to the end of a composition.
 */
export function compositionAppendChild(document: number, node: types.NodeHandle, child: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    check(exports().otio_composition_append_child(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the child of a composition that covers an instant.
 *
 * `shallow` stops at the composition's own children rather than descending
 * into nested ones. Answers `undefined` when nothing covers it.
 */
export function compositionChildAtTime(document: number, node: types.NodeHandle, time: values.RationalTimeLike, shallow: boolean): types.NodeHandle | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $arg1 = shallow ? 1 : 0;
    const $out0 = $stack.alloc(8, 4);
    const $status = exports().otio_composition_child_at_time(document, $receiver, $arg0, $arg1, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the children of a composition that touch a span.
 */
export function compositionChildrenInRange(document: number, node: types.NodeHandle, searchRange: values.TimeRangeLike): types.NodeHandle[] {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, searchRange);
    const $count = $stack.alloc(4, 4);
    let $out0 = 0;
    let $capacity = 0;
    check(exports().otio_composition_children_in_range(document, $receiver, $arg0, $out0, $capacity, $count));
    $capacity = $stack.view.getUint32($count, true);
    $out0 = $stack.alloc($capacity * 8, 4);
    check(exports().otio_composition_children_in_range(document, $receiver, $arg0, $out0, $capacity, $count));
    const $found = $stack.view.getUint32($count, true);
    return Array.from({ length: $found }, (_unused, $index) => types.readNodeHandle($stack.view, $out0 + $index * 8));
  } finally {
    $stack.close();
  }
}

/**
 * Removes every child of a composition, and returns them.
 */
export function compositionClearChildren(document: number, node: types.NodeHandle): types.NodeHandle[] {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $count = $stack.alloc(4, 4);
    let $out0 = 0;
    let $capacity = 0;
    check(exports().otio_composition_clear_children(document, $receiver, $out0, $capacity, $count));
    $capacity = $stack.view.getUint32($count, true);
    $out0 = $stack.alloc($capacity * 8, 4);
    check(exports().otio_composition_clear_children(document, $receiver, $out0, $capacity, $count));
    const $found = $stack.view.getUint32($count, true);
    return Array.from({ length: $found }, (_unused, $index) => types.readNodeHandle($stack.view, $out0 + $index * 8));
  } finally {
    $stack.close();
  }
}

/**
 * Removes a child by handle.
 */
export function compositionDetachChild(document: number, node: types.NodeHandle, child: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    check(exports().otio_composition_detach_child(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns every object of a kind at or below a composition.
 *
 * A null `search_range` searches all of it. `shallow` stops the walk at the
 * composition's own children.
 */
export function compositionFindChildrenOfKind(document: number, node: types.NodeHandle, kind: types.NodeKind, searchRange: values.TimeRangeLike | undefined, shallow: boolean): types.NodeHandle[] {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = types.encodeNodeKind(kind);
    const $arg1 = searchRange === undefined ? 0 : $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, searchRange);
    const $arg2 = shallow ? 1 : 0;
    const $count = $stack.alloc(4, 4);
    let $out0 = 0;
    let $capacity = 0;
    check(exports().otio_composition_find_children_of_kind(document, $receiver, $arg0, $arg1, $arg2, $out0, $capacity, $count));
    $capacity = $stack.view.getUint32($count, true);
    $out0 = $stack.alloc($capacity * 8, 4);
    check(exports().otio_composition_find_children_of_kind(document, $receiver, $arg0, $arg1, $arg2, $out0, $capacity, $count));
    const $found = $stack.view.getUint32($count, true);
    return Array.from({ length: $found }, (_unused, $index) => types.readNodeHandle($stack.view, $out0 + $index * 8));
  } finally {
    $stack.close();
  }
}

/**
 * Returns how much unused media a child has on each side.
 */
export function compositionHandlesOfChild(document: number, node: types.NodeHandle, child: types.NodeHandle): types.Handles {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    const $out0 = $stack.alloc(48, 8);
    check(exports().otio_composition_handles_of_child(document, $receiver, $arg0, $out0));
    return types.readHandles($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a composition holds an object directly.
 */
export function compositionHasChild(document: number, node: types.NodeHandle, child: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_composition_has_child(document, $receiver, $arg0, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns where a child sits in its composition.
 */
export function compositionIndexOfChild(document: number, node: types.NodeHandle, child: types.NodeHandle): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_composition_index_of_child(document, $receiver, $arg0, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Adds a child to a composition at an index.
 *
 * A negative index counts from the end, as upstream's Python does. The child
 * must not already be in a composition.
 */
export function compositionInsertChild(document: number, node: types.NodeHandle, index: number, child: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = BigInt(index);
    const $arg1 = $stack.node(child);
    check(exports().otio_composition_insert_child(document, $receiver, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether an object descends from a composition at any depth.
 */
export function compositionIsParentOf(document: number, node: types.NodeHandle, other: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(other);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_composition_is_parent_of(document, $receiver, $arg0, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the children on either side of one, or
 * `undefined` where there is none.
 *
 * With `OTIO_NEIGHBOR_GAP_AROUND_TRANSITIONS`, a transition at the head or
 * tail of a track gets a gap materialized for its overhang, which is why this
 * takes a document it may edit.
 */
export function compositionNeighborsOf(document: number, node: types.NodeHandle, child: types.NodeHandle, policy: types.NeighborGapPolicy): { before: types.NodeHandle; after: types.NodeHandle } {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    const $arg1 = types.encodeNeighborGapPolicy(policy);
    const $out0 = $stack.alloc(8, 4);
    const $out1 = $stack.alloc(8, 4);
    check(exports().otio_composition_neighbors_of(document, $receiver, $arg0, $arg1, $out0, $out1));
    return {
      before: types.readNodeHandle($stack.view, $out0),
      after: types.readNodeHandle($stack.view, $out1),
    };
  } finally {
    $stack.close();
  }
}

/**
 * Creates a bare composition: children with no layout of its own.
 */
export function compositionNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_composition_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns where a child sits in a composition's clock, at any depth.
 */
export function compositionRangeOfChild(document: number, node: types.NodeHandle, child: types.NodeHandle): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_composition_range_of_child(document, $receiver, $arg0, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns where the child at an index sits in its composition's clock.
 *
 * A negative index counts from the end.
 */
export function compositionRangeOfChildAtIndex(document: number, node: types.NodeHandle, index: number): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = BigInt(index);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_composition_range_of_child_at_index(document, $receiver, $arg0, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns where every child of a composition sits, in one pass.
 *
 * `out_nodes` and `out_ranges` are filled in step, so entry `i` of one goes
 * with entry `i` of the other. Either may be null when only the count is
 * wanted.
 */
export function compositionRangesOfChildren(document: number, node: types.NodeHandle): { nodes: types.NodeHandle[]; ranges: values.TimeRange[] } {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $count = $stack.alloc(4, 4);
    let $out0 = 0;
    let $out1 = 0;
    let $capacity = 0;
    check(exports().otio_composition_ranges_of_children(document, $receiver, $out0, $out1, $capacity, $count));
    $capacity = $stack.view.getUint32($count, true);
    $out0 = $stack.alloc($capacity * 8, 4);
    $out1 = $stack.alloc($capacity * 32, 8);
    check(exports().otio_composition_ranges_of_children(document, $receiver, $out0, $out1, $capacity, $count));
    const $found = $stack.view.getUint32($count, true);
    return {
      nodes: Array.from({ length: $found }, (_unused, $index) => types.readNodeHandle($stack.view, $out0 + $index * 8)),
      ranges: Array.from({ length: $found }, (_unused, $index) => types.readTimeRange($stack.view, $out1 + $index * 32)),
    };
  } finally {
    $stack.close();
  }
}

/**
 * Removes a child by index, and returns it.
 *
 * The child stays in the document with no parent.
 */
export function compositionRemoveChild(document: number, node: types.NodeHandle, index: number): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = BigInt(index);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_composition_remove_child(document, $receiver, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Clips a range to a composition's own trim.
 *
 * Answers `undefined` when nothing of it is left.
 */
export function compositionTrimChildRange(document: number, node: types.NodeHandle, childRange: values.TimeRangeLike): values.TimeRange | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, childRange);
    const $out0 = $stack.alloc(32, 8);
    const $status = exports().otio_composition_trim_child_range(document, $receiver, $arg0, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns where a child sits after the composition's trim, at any depth.
 *
 * Answers `undefined` when the trim excludes it entirely.
 */
export function compositionTrimmedRangeOfChild(document: number, node: types.NodeHandle, child: types.NodeHandle): values.TimeRange | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(child);
    const $out0 = $stack.alloc(32, 8);
    const $status = exports().otio_composition_trimmed_range_of_child(document, $receiver, $arg0, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns where the child at an index sits, after the composition's own trim.
 */
export function compositionTrimmedRangeOfChildAtIndex(document: number, node: types.NodeHandle, index: number): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = BigInt(index);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_composition_trimmed_range_of_child_at_index(document, $receiver, $arg0, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * The tolerance, in seconds, that the range predicates use by default.
 *
 * Upstream's C++ takes this as a default argument; C has none, so the value
 * is a call rather than a constant in the header, which keeps the two from
 * drifting apart.
 */
export function defaultEpsilonS(): number {
  const $stack = openStack();
  try {
    return exports().otio_default_epsilon_s();
  } finally {
    $stack.close();
  }
}

/**
 * Returns the indentation upstream's Python bindings write by default.
 */
export function defaultIndent(): number {
  const $stack = openStack();
  try {
    return exports().otio_default_indent();
  } finally {
    $stack.close();
  }
}

/**
 * Copies a document, objects and all.
 *
 * Handles into the original name the same objects in the copy, because the
 * copy keeps the arena's layout.
 */
export function documentClone(document: number): number {
  const $stack = openStack();
  try {
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_document_clone(document, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a handle still names a live object.
 */
export function documentContains(document: number, node: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    return exports().otio_document_contains(document, $receiver) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Copies an object and everything it owns, into the same document.
 *
 * The copy has no parent, whatever the original had.
 */
export function documentDeepClone(document: number, node: types.NodeHandle): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_document_deep_clone(document, $receiver, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Releases a document and every object in it.
 *
 * Passing null does nothing. Every handle into the document is stale
 * afterwards; using one is a programming error this library cannot detect,
 * because the arena it would be checked against is gone.
 */
export function documentFree(document: number): void {
  const $stack = openStack();
  try {
    exports().otio_document_free(document);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a document from OTIO JSON.
 */
export function documentFromJson(json: string): number {
  const $stack = openStack();
  try {
    const $arg0 = $stack.text(json);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_document_from_json($arg0, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Creates an empty document with no root.
 *
 * Returns null only if the allocation fails. Release it with
 * `Timeline#dispose`.
 */
export function documentNew(): number {
  const $stack = openStack();
  try {
    return exports().otio_document_new();
  } finally {
    $stack.close();
  }
}

/**
 * Returns how many live objects the document holds.
 */
export function documentNodeCount(document: number): number {
  const $stack = openStack();
  try {
    return exports().otio_document_node_count(document);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a document from a `.otio` file on disk.
 */
export function documentReadFromFile(path: string): number {
  const $stack = openStack();
  try {
    const $arg0 = $stack.text(path);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_document_read_from_file($arg0, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Removes one object from the document.
 *
 * Anything that referred to it still holds a handle, and that handle is now
 * stale: a lookup fails rather than reaching whatever takes the slot next. To
 * remove an object together with everything hanging off it, use
 * `Node#removeRecursive`.
 */
export function documentRemove(document: number, node: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    check(exports().otio_document_remove(document, $receiver));
  } finally {
    $stack.close();
  }
}

/**
 * Removes an object and everything it owns: children, markers, effects and
 * media references.
 */
export function documentRemoveRecursive(document: number, node: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    check(exports().otio_document_remove_recursive(document, $receiver));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the document's root object.
 *
 * Answers `undefined` for a document that has none, which is what
 * a freshly created one is.
 */
export function documentRoot(document: number): types.NodeHandle | undefined {
  const $stack = openStack();
  try {
    const $out0 = $stack.alloc(8, 4);
    const $status = exports().otio_document_root(document, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets the document's root object.
 *
 * Passing `undefined` clears it.
 */
export function documentSetRoot(document: number, node: types.NodeHandle | undefined): void {
  const $stack = openStack();
  try {
    const $arg0 = node === undefined ? $stack.noneNode() : $stack.node(node);
    check(exports().otio_document_set_root(document, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a document as OTIO JSON, starting from its root.
 *
 * `indent` is how many spaces each level is indented by;
 * `defaultIndent()` is what upstream's Python bindings use.
 */
export function documentToJson(document: number, indent: number): string {
  const $stack = openStack();
  try {
    const $arg0 = indent;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_document_to_json(document, $arg0, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Writes a document to a `.otio` file on disk.
 */
export function documentWriteToFile(document: number, path: string, indent: number): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.text(path);
    const $arg1 = indent;
    check(exports().otio_document_write_to_file(document, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Drops an item into a gap on a track, fitting it as the reference point
 * says.
 */
export function editFill(document: number, item: types.NodeHandle, track: types.NodeHandle, trackTime: values.RationalTimeLike, referencePoint: types.ReferencePoint): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.node(track);
    const $arg2 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, trackTime);
    const $arg3 = types.encodeReferencePoint(referencePoint);
    check(exports().otio_edit_fill(document, $arg0, $arg1, $arg2, $arg3));
  } finally {
    $stack.close();
  }
}

/**
 * Inserts an item at an instant, pushing what follows later.
 */
export function editInsert(document: number, item: types.NodeHandle, composition: types.NodeHandle, time: values.RationalTimeLike, removeTransitions: boolean, fillTemplate: types.NodeHandle | undefined): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.node(composition);
    const $arg2 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $arg3 = removeTransitions ? 1 : 0;
    const $arg4 = fillTemplate === undefined ? $stack.noneNode() : $stack.node(fillTemplate);
    check(exports().otio_edit_insert(document, $arg0, $arg1, $arg2, $arg3, $arg4));
  } finally {
    $stack.close();
  }
}

/**
 * Lays an item over a span of a composition, replacing what was there.
 *
 * `fill_template` is the item to fill any gap the edit opens with, or
 * `undefined` for a plain gap.
 */
export function editOverwrite(document: number, item: types.NodeHandle, composition: types.NodeHandle, range: values.TimeRangeLike, removeTransitions: boolean, fillTemplate: types.NodeHandle | undefined): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.node(composition);
    const $arg2 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, range);
    const $arg3 = removeTransitions ? 1 : 0;
    const $arg4 = fillTemplate === undefined ? $stack.noneNode() : $stack.node(fillTemplate);
    check(exports().otio_edit_overwrite(document, $arg0, $arg1, $arg2, $arg3, $arg4));
  } finally {
    $stack.close();
  }
}

/**
 * Takes whatever sits at an instant out of a composition.
 *
 * With `fill` set, a gap takes its place; without, what follows moves up.
 */
export function editRemove(document: number, composition: types.NodeHandle, time: values.RationalTimeLike, fill: boolean, fillTemplate: types.NodeHandle | undefined): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(composition);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $arg2 = fill ? 1 : 0;
    const $arg3 = fillTemplate === undefined ? $stack.noneNode() : $stack.node(fillTemplate);
    check(exports().otio_edit_remove(document, $arg0, $arg1, $arg2, $arg3));
  } finally {
    $stack.close();
  }
}

/**
 * Moves an item's in and out points, sliding everything after it.
 */
export function editRipple(document: number, item: types.NodeHandle, deltaIn: values.RationalTimeLike, deltaOut: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, deltaIn);
    const $arg2 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, deltaOut);
    check(exports().otio_edit_ripple(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Moves the cut between an item and its neighbour.
 */
export function editRoll(document: number, item: types.NodeHandle, deltaIn: values.RationalTimeLike, deltaOut: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, deltaIn);
    const $arg2 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, deltaOut);
    check(exports().otio_edit_roll(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Cuts whatever sits at an instant into two.
 */
export function editSlice(document: number, composition: types.NodeHandle, time: values.RationalTimeLike, removeTransitions: boolean): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(composition);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $arg2 = removeTransitions ? 1 : 0;
    check(exports().otio_edit_slice(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Moves an item along its track, taking the time from its neighbours.
 */
export function editSlide(document: number, item: types.NodeHandle, delta: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, delta);
    check(exports().otio_edit_slide(document, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Moves the media inside an item without moving the item.
 */
export function editSlip(document: number, item: types.NodeHandle, delta: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, delta);
    check(exports().otio_edit_slip(document, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Moves an item's in and out points without moving its neighbours.
 */
export function editTrim(document: number, item: types.NodeHandle, deltaIn: values.RationalTimeLike, deltaOut: values.RationalTimeLike, fillTemplate: types.NodeHandle | undefined): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(item);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, deltaIn);
    const $arg2 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, deltaOut);
    const $arg3 = fillTemplate === undefined ? $stack.noneNode() : $stack.node(fillTemplate);
    check(exports().otio_edit_trim(document, $arg0, $arg1, $arg2, $arg3));
  } finally {
    $stack.close();
  }
}

/**
 * Returns an effect's own name, such as `"LinearTimeWarp"`.
 */
export function effectEffectName(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_effect_effect_name(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether an effect is applied.
 */
export function effectEnabled(document: number, node: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_effect_enabled(document, $receiver, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Creates an effect. `effect_name` is the effect's own name, such as
 * `"Blur"`, which is separate from the object's name.
 */
export function effectNew(document: number, name: string | undefined, effectName: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = effectName === undefined ? 0 : $stack.text(effectName);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_effect_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets an effect's own name.
 */
export function effectSetEffectName(document: number, node: types.NodeHandle, effectName: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(effectName);
    check(exports().otio_effect_set_effect_name(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets whether an effect is applied.
 */
export function effectSetEnabled(document: number, node: types.NodeHandle, enabled: boolean): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = enabled ? 1 : 0;
    check(exports().otio_effect_set_enabled(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets a speed change's multiplier.
 */
export function effectSetTimeScalar(document: number, node: types.NodeHandle, scalar: number): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = scalar;
    check(exports().otio_effect_set_time_scalar(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns a speed change's multiplier.
 *
 * Only a `LinearTimeWarp` and a `FreezeFrame` have one; anything else reports
 * an `OtioError` whose status is `"coreError"`.
 */
export function effectTimeScalar(document: number, node: types.NodeHandle): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 8);
    check(exports().otio_effect_time_scalar(document, $receiver, $out0));
    return $stack.view.getFloat64($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the message describing the last failing call on this thread.
 *
 * The string is owned by the library and stays valid until the next `otio_*`
 * call on this thread. After a call that succeeded it is empty. It is never
 * null.
 */
export function errorMessage(): string {
  const $stack = openStack();
  try {
    return readCString(exports().otio_error_message());
  } finally {
    $stack.close();
  }
}

/**
 * Creates a media reference pointing at a URL.
 */
export function externalReferenceNew(document: number, name: string | undefined, targetUrl: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = targetUrl === undefined ? 0 : $stack.text(targetUrl);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_external_reference_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets where an external reference's media lives.
 */
export function externalReferenceSetTargetUrl(document: number, node: types.NodeHandle, url: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(url);
    check(exports().otio_external_reference_set_target_url(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns where an external reference's media lives.
 */
export function externalReferenceTargetUrl(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_external_reference_target_url(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns the format that claims a filename suffix, such as `"edl"`.
 *
 * The suffix is matched without its dot and without regard to case. Reports
 * `undefined` for a suffix no format claims.
 */
export function formatFromSuffix(suffix: string): types.Format | undefined {
  const $stack = openStack();
  try {
    const $arg0 = $stack.text(suffix);
    const $out0 = $stack.alloc(4, 4);
    const $status = exports().otio_format_from_suffix($arg0, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.decodeFormat($stack.view.getInt32($out0, true));
  } finally {
    $stack.close();
  }
}

/**
 * Returns an adapter's name, as upstream's plugin manifest spells it.
 *
 * The string is static and needs no freeing.
 */
export function formatName(format: types.Format): string {
  const $stack = openStack();
  try {
    const $arg0 = types.encodeFormat(format);
    return readCString(exports().otio_format_name($arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Creates a freeze frame: a hold on a single frame.
 */
export function freezeFrameNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_freeze_frame_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a gap.
 */
export function gapNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_gap_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns which generator a generator reference names, such as
 * `"SMPTEBars"`.
 */
export function generatorReferenceKind(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_generator_reference_kind(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Creates a media reference for generated media, such as colour bars.
 */
export function generatorReferenceNew(document: number, name: string | undefined, generatorKind: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = generatorKind === undefined ? 0 : $stack.text(generatorKind);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_generator_reference_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets which generator a generator reference names.
 */
export function generatorReferenceSetKind(document: number, node: types.NodeHandle, kind: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(kind);
    check(exports().otio_generator_reference_set_kind(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the part of each frame's filename before the frame number.
 */
export function imageSequenceReferenceNamePrefix(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_image_sequence_reference_name_prefix(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns the part of each frame's filename after the frame number.
 */
export function imageSequenceReferenceNameSuffix(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_image_sequence_reference_name_suffix(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Creates a media reference for a numbered sequence of image files.
 *
 * The filename parts and the numbers start empty and at zero; set them with
 * `ImageSequenceReference#numbers` and the calls beside it.
 */
export function imageSequenceReferenceNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_image_sequence_reference_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the numbers describing how an image sequence is laid out.
 */
export function imageSequenceReferenceNumbers(document: number, node: types.NodeHandle): types.ImageSequence {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(40, 8);
    check(exports().otio_image_sequence_reference_numbers(document, $receiver, $out0));
    return types.readImageSequence($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets the part of each frame's filename before the frame number.
 */
export function imageSequenceReferenceSetNamePrefix(document: number, node: types.NodeHandle, prefix: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(prefix);
    check(exports().otio_image_sequence_reference_set_name_prefix(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the part of each frame's filename after the frame number.
 */
export function imageSequenceReferenceSetNameSuffix(document: number, node: types.NodeHandle, suffix: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(suffix);
    check(exports().otio_image_sequence_reference_set_name_suffix(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the numbers describing how an image sequence is laid out.
 */
export function imageSequenceReferenceSetNumbers(document: number, node: types.NodeHandle, numbers: types.ImageSequence): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeImageSequence, types.sizeOfImageSequence, types.alignOfImageSequence, numbers);
    check(exports().otio_image_sequence_reference_set_numbers(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the directory an image sequence's frames sit in.
 */
export function imageSequenceReferenceSetTargetUrlBase(document: number, node: types.NodeHandle, urlBase: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(urlBase);
    check(exports().otio_image_sequence_reference_set_target_url_base(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the directory an image sequence's frames sit in.
 */
export function imageSequenceReferenceTargetUrlBase(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_image_sequence_reference_target_url_base(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a rate is a drop-frame rate.
 */
export function isDropFrameRate(rate: number): boolean {
  const $stack = openStack();
  try {
    const $arg0 = rate;
    return exports().otio_is_drop_frame_rate($arg0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a rate is one SMPTE timecode is defined for.
 */
export function isSmpteTimecodeRate(rate: number): boolean {
  const $stack = openStack();
  try {
    const $arg0 = rate;
    return exports().otio_is_smpte_timecode_rate($arg0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Adds an effect to an item. Effects apply in the order they are added.
 */
export function itemAppendEffect(document: number, node: types.NodeHandle, effectHandle: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(effectHandle);
    check(exports().otio_item_append_effect(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Adds a marker to an item.
 */
export function itemAppendMarker(document: number, node: types.NodeHandle, markerHandle: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.node(markerHandle);
    check(exports().otio_item_append_marker(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the span of media an object could draw on, before trimming.
 */
export function itemAvailableRange(document: number, node: types.NodeHandle): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_item_available_range(document, $receiver, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Clears an item's display tint.
 */
export function itemClearColor(document: number, node: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    check(exports().otio_item_clear_color(document, $receiver));
  } finally {
    $stack.close();
  }
}

/**
 * Clears an item's source range, so that it takes all of its media.
 */
export function itemClearSourceRange(document: number, node: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    check(exports().otio_item_clear_source_range(document, $receiver));
  } finally {
    $stack.close();
  }
}

/**
 * Returns an item's display tint, and the name that goes with it.
 *
 * `out_name` may be null if the name is not wanted. Reports
 * `undefined` for an untinted item.
 */
export function itemColor(document: number, node: types.NodeHandle): { color: types.Color; name: string } | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    const $out1 = $stack.alloc(8, 4);
    const $status = exports().otio_item_color(document, $receiver, $out0, $out1);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return {
      color: types.readColor($stack.view, $out0),
      name: readBuffer($out1, "text"),
    };
  } finally {
    $stack.close();
  }
}

/**
 * Returns how long an object occupies its parent's timeline.
 */
export function itemDuration(document: number, node: types.NodeHandle): values.RationalTime {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_item_duration(document, $receiver, $out0));
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns one of an item's effects.
 */
export function itemEffectAt(document: number, node: types.NodeHandle, index: number): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = index;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_item_effect_at(document, $receiver, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns how many effects an item carries.
 */
export function itemEffectCount(document: number, node: types.NodeHandle): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_item_effect_count(document, $receiver, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether an item contributes to its composition.
 */
export function itemEnabled(document: number, node: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_item_enabled(document, $receiver, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns one of an item's markers.
 */
export function itemMarkerAt(document: number, node: types.NodeHandle, index: number): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = index;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_item_marker_at(document, $receiver, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns how many markers an item carries.
 */
export function itemMarkerCount(document: number, node: types.NodeHandle): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_item_marker_count(document, $receiver, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a bare item: something that occupies time without saying what fills
 * it.
 */
export function itemNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_item_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns where an object sits in its parent's clock.
 */
export function itemRangeInParent(document: number, node: types.NodeHandle): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_item_range_in_parent(document, $receiver, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Removes one of an item's effects, and returns it.
 */
export function itemRemoveEffect(document: number, node: types.NodeHandle, index: number): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = index;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_item_remove_effect(document, $receiver, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Removes one of an item's markers, and returns it.
 *
 * The marker stays in the document; remove it with
 * `Node#removeRecursive`
 * if nothing else holds it.
 */
export function itemRemoveMarker(document: number, node: types.NodeHandle, index: number): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = index;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_item_remove_marker(document, $receiver, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets an item's display tint. `name` may be null for an unnamed colour.
 */
export function itemSetColor(document: number, node: types.NodeHandle, color: types.Color, name: string | undefined): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeColor, types.sizeOfColor, types.alignOfColor, color);
    const $arg1 = name === undefined ? 0 : $stack.text(name);
    check(exports().otio_item_set_color(document, $receiver, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Sets whether an item contributes to its composition.
 */
export function itemSetEnabled(document: number, node: types.NodeHandle, enabled: boolean): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = enabled ? 1 : 0;
    check(exports().otio_item_set_enabled(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the portion of its media an item uses.
 */
export function itemSetSourceRange(document: number, node: types.NodeHandle, range: values.TimeRangeLike): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, range);
    check(exports().otio_item_set_source_range(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the portion of its media an item uses.
 *
 * Answers `undefined` for an item that takes all of it.
 */
export function itemSourceRange(document: number, node: types.NodeHandle): values.TimeRange | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    const $status = exports().otio_item_source_range(document, $receiver, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the span of media an object uses, in its own clock.
 */
export function itemTrimmedRange(document: number, node: types.NodeHandle): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_item_trimmed_range(document, $receiver, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns where an object sits in its parent's clock, after the parent's own
 * trim.
 *
 * Answers `undefined` when the parent's trim excludes the object
 * entirely.
 */
export function itemTrimmedRangeInParent(document: number, node: types.NodeHandle): values.TimeRange | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    const $status = exports().otio_item_trimmed_range_in_parent(document, $receiver, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the span of media an object shows, including what its transitions
 * reach into.
 */
export function itemVisibleRange(document: number, node: types.NodeHandle): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_item_visible_range(document, $receiver, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a constant-rate speed change. A `time_scalar` of 2.0 plays twice as
 * fast.
 */
export function linearTimeWarpNew(document: number, name: string | undefined, timeScalar: number): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = timeScalar;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_linear_time_warp_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns a marker's tint, and the name that goes with it.
 *
 * `out_name` may be null if the name is not wanted. Reports
 * `undefined` for an untinted marker.
 */
export function markerColor(document: number, node: types.NodeHandle): { color: types.Color; name: string } | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    const $out1 = $stack.alloc(8, 4);
    const $status = exports().otio_marker_color(document, $receiver, $out0, $out1);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return {
      color: types.readColor($stack.view, $out0),
      name: readBuffer($out1, "text"),
    };
  } finally {
    $stack.close();
  }
}

/**
 * Returns a marker's note.
 */
export function markerComment(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_marker_comment(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns the span a marker covers.
 */
export function markerMarkedRange(document: number, node: types.NodeHandle): values.TimeRange {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_marker_marked_range(document, $receiver, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a marker covering `marked_range`.
 */
export function markerNew(document: number, name: string | undefined, markedRange: values.TimeRangeLike): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, markedRange);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_marker_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets a marker's tint. `name` may be null for an unnamed colour.
 */
export function markerSetColor(document: number, node: types.NodeHandle, color: types.Color, name: string | undefined): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeColor, types.sizeOfColor, types.alignOfColor, color);
    const $arg1 = name === undefined ? 0 : $stack.text(name);
    check(exports().otio_marker_set_color(document, $receiver, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Sets a marker's note.
 */
export function markerSetComment(document: number, node: types.NodeHandle, comment: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(comment);
    check(exports().otio_marker_set_comment(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the span a marker covers.
 */
export function markerSetMarkedRange(document: number, node: types.NodeHandle, range: values.TimeRangeLike): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, range);
    check(exports().otio_marker_set_marked_range(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the image bounds a reference says its media has.
 */
export function mediaReferenceAvailableImageBounds(document: number, node: types.NodeHandle): types.Box2d | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    const $status = exports().otio_media_reference_available_image_bounds(document, $receiver, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readBox2d($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the span of media a reference says is available.
 *
 * Answers `undefined` for a reference that does not say.
 */
export function mediaReferenceAvailableRange(document: number, node: types.NodeHandle): values.TimeRange | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(32, 8);
    const $status = exports().otio_media_reference_available_range(document, $receiver, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Clears the image bounds a reference says its media has.
 */
export function mediaReferenceClearAvailableImageBounds(document: number, node: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    check(exports().otio_media_reference_clear_available_image_bounds(document, $receiver));
  } finally {
    $stack.close();
  }
}

/**
 * Clears the span of media a reference says is available.
 */
export function mediaReferenceClearAvailableRange(document: number, node: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    check(exports().otio_media_reference_clear_available_range(document, $receiver));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the image bounds a reference says its media has.
 */
export function mediaReferenceSetAvailableImageBounds(document: number, node: types.NodeHandle, bounds: types.Box2d): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeBox2d, types.sizeOfBox2d, types.alignOfBox2d, bounds);
    check(exports().otio_media_reference_set_available_image_bounds(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the span of media a reference says is available.
 */
export function mediaReferenceSetAvailableRange(document: number, node: types.NodeHandle, range: values.TimeRangeLike): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, range);
    check(exports().otio_media_reference_set_available_range(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Empties an object's metadata.
 */
export function metadataClear(document: number, nodeHandle: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    check(exports().otio_metadata_clear(document, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether anything sits at a path.
 */
export function metadataContains(document: number, nodeHandle: types.NodeHandle, path: string): boolean {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_metadata_contains(document, $arg0, $arg1, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Reads a boolean from the metadata.
 */
export function metadataGetBool(document: number, nodeHandle: types.NodeHandle, path: string): boolean {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_metadata_get_bool(document, $arg0, $arg1, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Reads a rectangle from the metadata.
 */
export function metadataGetBox2d(document: number, nodeHandle: types.NodeHandle, path: string): types.Box2d {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_metadata_get_box2d(document, $arg0, $arg1, $out0));
    return types.readBox2d($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a colour, and the name that goes with it, from the metadata.
 *
 * `out_name` may be null if the name is not wanted.
 */
export function metadataGetColor(document: number, nodeHandle: types.NodeHandle, path: string): { value: types.Color; name: string } {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(32, 8);
    const $out1 = $stack.alloc(8, 4);
    check(exports().otio_metadata_get_color(document, $arg0, $arg1, $out0, $out1));
    return {
      value: types.readColor($stack.view, $out0),
      name: readBuffer($out1, "text"),
    };
  } finally {
    $stack.close();
  }
}

/**
 * Reads a number from the metadata.
 */
export function metadataGetDouble(document: number, nodeHandle: types.NodeHandle, path: string): number {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(8, 8);
    check(exports().otio_metadata_get_double(document, $arg0, $arg1, $out0));
    return $stack.view.getFloat64($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a signed integer from the metadata.
 */
export function metadataGetInt(document: number, nodeHandle: types.NodeHandle, path: string): number {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(8, 8);
    check(exports().otio_metadata_get_int(document, $arg0, $arg1, $out0));
    return Number($stack.view.getBigInt64($out0, true));
  } finally {
    $stack.close();
  }
}

/**
 * Reads a handle to an OTIO object held in the metadata.
 */
export function metadataGetObject(document: number, nodeHandle: types.NodeHandle, path: string): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_metadata_get_object(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a time from the metadata.
 */
export function metadataGetRationalTime(document: number, nodeHandle: types.NodeHandle, path: string): values.RationalTime {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_metadata_get_rational_time(document, $arg0, $arg1, $out0));
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a string from the metadata.
 */
export function metadataGetString(document: number, nodeHandle: types.NodeHandle, path: string): string {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_metadata_get_string(document, $arg0, $arg1, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Reads a span from the metadata.
 */
export function metadataGetTimeRange(document: number, nodeHandle: types.NodeHandle, path: string): values.TimeRange {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_metadata_get_time_range(document, $arg0, $arg1, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a transform from the metadata.
 */
export function metadataGetTimeTransform(document: number, nodeHandle: types.NodeHandle, path: string): values.TimeTransform {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_metadata_get_time_transform(document, $arg0, $arg1, $out0));
    return types.readTimeTransform($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Reads an unsigned integer from the metadata.
 */
export function metadataGetUint(document: number, nodeHandle: types.NodeHandle, path: string): number {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(8, 8);
    check(exports().otio_metadata_get_uint(document, $arg0, $arg1, $out0));
    return Number($stack.view.getBigUint64($out0, true));
  } finally {
    $stack.close();
  }
}

/**
 * Reads a point from the metadata.
 */
export function metadataGetV2d(document: number, nodeHandle: types.NodeHandle, path: string): types.V2d {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_metadata_get_v2d(document, $arg0, $arg1, $out0));
    return types.readV2d($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the key at an index of a dictionary at a path.
 *
 * Keys are ordered, so walking the indices walks them the same way twice.
 */
export function metadataKeyAt(document: number, nodeHandle: types.NodeHandle, path: string, index: number): string | undefined {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = index;
    const $out0 = $stack.alloc(8, 4);
    const $status = exports().otio_metadata_key_at(document, $arg0, $arg1, $arg2, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns what kind of value sits at a path.
 *
 * An empty or null path names the metadata dictionary itself, which is always
 * `OTIO_VALUE_DICTIONARY`.
 */
export function metadataKind(document: number, nodeHandle: types.NodeHandle, path: string): types.ValueKind | undefined {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(4, 4);
    const $status = exports().otio_metadata_kind(document, $arg0, $arg1, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.decodeValueKind($stack.view.getInt32($out0, true));
  } finally {
    $stack.close();
  }
}

/**
 * Returns how many entries a dictionary or an array at a path holds.
 */
export function metadataLen(document: number, nodeHandle: types.NodeHandle, path: string): number | undefined {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $out0 = $stack.alloc(4, 4);
    const $status = exports().otio_metadata_len(document, $arg0, $arg1, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Removes whatever sits at a path.
 *
 * Removing a key of a dictionary takes the key with it; removing an element
 * of an array shortens the array.
 */
export function metadataRemove(document: number, nodeHandle: types.NodeHandle, path: string): void | undefined {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $status = exports().otio_metadata_remove(document, $arg0, $arg1);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
  } finally {
    $stack.close();
  }
}

/**
 * Writes a boolean into the metadata.
 */
export function metadataSetBool(document: number, nodeHandle: types.NodeHandle, path: string, value: boolean): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = value ? 1 : 0;
    check(exports().otio_metadata_set_bool(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a rectangle into the metadata.
 */
export function metadataSetBox2d(document: number, nodeHandle: types.NodeHandle, path: string, value: types.Box2d): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.record(types.writeBox2d, types.sizeOfBox2d, types.alignOfBox2d, value);
    check(exports().otio_metadata_set_box2d(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a colour into the metadata. `name` may be null for an unnamed one.
 */
export function metadataSetColor(document: number, nodeHandle: types.NodeHandle, path: string, value: types.Color, name: string | undefined): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.record(types.writeColor, types.sizeOfColor, types.alignOfColor, value);
    const $arg3 = name === undefined ? 0 : $stack.text(name);
    check(exports().otio_metadata_set_color(document, $arg0, $arg1, $arg2, $arg3));
  } finally {
    $stack.close();
  }
}

/**
 * Writes an empty dictionary into the metadata, to be filled through deeper
 * paths.
 */
export function metadataSetDictionary(document: number, nodeHandle: types.NodeHandle, path: string): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    check(exports().otio_metadata_set_dictionary(document, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a number into the metadata.
 */
export function metadataSetDouble(document: number, nodeHandle: types.NodeHandle, path: string, value: number): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = value;
    check(exports().otio_metadata_set_double(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a signed integer into the metadata.
 */
export function metadataSetInt(document: number, nodeHandle: types.NodeHandle, path: string, value: number): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = BigInt(value);
    check(exports().otio_metadata_set_int(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a null into the metadata.
 */
export function metadataSetNull(document: number, nodeHandle: types.NodeHandle, path: string): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    check(exports().otio_metadata_set_null(document, $arg0, $arg1));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a handle to an OTIO object into the metadata.
 *
 * The object stays where it is and the metadata names it. Nothing checks
 * that the handle is live, because a document being built may not hold the
 * object yet.
 */
export function metadataSetObject(document: number, nodeHandle: types.NodeHandle, path: string, value: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.node(value);
    check(exports().otio_metadata_set_object(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a time into the metadata.
 */
export function metadataSetRationalTime(document: number, nodeHandle: types.NodeHandle, path: string, value: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, value);
    check(exports().otio_metadata_set_rational_time(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a string into the metadata.
 */
export function metadataSetString(document: number, nodeHandle: types.NodeHandle, path: string, value: string): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.text(value);
    check(exports().otio_metadata_set_string(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a span into the metadata.
 */
export function metadataSetTimeRange(document: number, nodeHandle: types.NodeHandle, path: string, value: values.TimeRangeLike): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, value);
    check(exports().otio_metadata_set_time_range(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a transform into the metadata.
 */
export function metadataSetTimeTransform(document: number, nodeHandle: types.NodeHandle, path: string, value: values.TimeTransformLike): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.record(types.writeTimeTransform, types.sizeOfTimeTransform, types.alignOfTimeTransform, value);
    check(exports().otio_metadata_set_time_transform(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes an unsigned integer into the metadata.
 */
export function metadataSetUint(document: number, nodeHandle: types.NodeHandle, path: string, value: number): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = BigInt(value);
    check(exports().otio_metadata_set_uint(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes a point into the metadata.
 */
export function metadataSetV2d(document: number, nodeHandle: types.NodeHandle, path: string, value: types.V2d): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = $stack.record(types.writeV2d, types.sizeOfV2d, types.alignOfV2d, value);
    check(exports().otio_metadata_set_v2d(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Writes an array of `len` nulls into the metadata, to be filled by index.
 */
export function metadataSetVector(document: number, nodeHandle: types.NodeHandle, path: string, len: number): void {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(nodeHandle);
    const $arg1 = $stack.text(path);
    const $arg2 = len;
    check(exports().otio_metadata_set_vector(document, $arg0, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

/**
 * Creates a media reference for media known to exist somewhere unknown.
 */
export function missingReferenceNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_missing_reference_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the SMPTE timecode rate closest to a rate.
 */
export function nearestSmpteTimecodeRate(rate: number): number {
  const $stack = openStack();
  try {
    const $arg0 = rate;
    return exports().otio_nearest_smpte_timecode_rate($arg0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns one of an object's children.
 */
export function nodeChildAt(document: number, node: types.NodeHandle, index: number): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = index;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_node_child_at(document, $receiver, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns how many children an object holds.
 *
 * Tracks, stacks, bare compositions and serializable collections hold
 * children; anything else reports an `OtioError` whose status is `"coreError"`.
 */
export function nodeChildCount(document: number, node: types.NodeHandle): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_node_child_count(document, $receiver, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Returns an object's children.
 */
export function nodeChildren(document: number, node: types.NodeHandle): types.NodeHandle[] {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $count = $stack.alloc(4, 4);
    let $out0 = 0;
    let $capacity = 0;
    check(exports().otio_node_children(document, $receiver, $out0, $capacity, $count));
    $capacity = $stack.view.getUint32($count, true);
    $out0 = $stack.alloc($capacity * 8, 4);
    check(exports().otio_node_children(document, $receiver, $out0, $capacity, $count));
    const $found = $stack.view.getUint32($count, true);
    return Array.from({ length: $found }, (_unused, $index) => types.readNodeHandle($stack.view, $out0 + $index * 8));
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether two handles name the same object.
 *
 * This is object identity: two handles are equal only if they were issued for
 * the same occupant of the same slot.
 */
export function nodeEqual(left: types.NodeHandle, right: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(left);
    const $arg1 = $stack.node(right);
    return exports().otio_node_equal($arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns every clip at or below an object, in order.
 */
export function nodeFindClips(document: number, node: types.NodeHandle): types.NodeHandle[] {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $count = $stack.alloc(4, 4);
    let $out0 = 0;
    let $capacity = 0;
    check(exports().otio_node_find_clips(document, $receiver, $out0, $capacity, $count));
    $capacity = $stack.view.getUint32($count, true);
    $out0 = $stack.alloc($capacity * 8, 4);
    check(exports().otio_node_find_clips(document, $receiver, $out0, $capacity, $count));
    const $found = $stack.view.getUint32($count, true);
    return Array.from({ length: $found }, (_unused, $index) => types.readNodeHandle($stack.view, $out0 + $index * 8));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the outermost object above this one.
 */
export function nodeHighestAncestor(document: number, node: types.NodeHandle): types.NodeHandle {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_node_highest_ancestor(document, $receiver, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a handle names no object.
 */
export function nodeIsNone(node: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $arg0 = $stack.node(node);
    return exports().otio_node_is_none($arg0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns what kind of object a handle names.
 */
export function nodeKind(document: number, node: types.NodeHandle): types.NodeKind {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_node_kind(document, $receiver, $out0));
    return types.decodeNodeKind($stack.view.getInt32($out0, true));
  } finally {
    $stack.close();
  }
}

/**
 * Returns an object's name, which is empty for an object that has none.
 */
export function nodeName(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_node_name(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns the handle that names no object.
 *
 * Fields that may be absent, such as a timeline's tracks before one is set,
 * report this. Compare against it with
 * `undefined`.
 */
export function nodeNone(): types.NodeHandle {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(8, 4); /* OtioNode */
    exports().otio_node_none($sret);
    return types.readNodeHandle($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether an object sits over its neighbours rather than beside them.
 *
 * Only a transition does, which is why one does not advance the playhead when
 * a track is laid out.
 */
export function nodeOverlapping(document: number, node: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_node_overlapping(document, $receiver, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the composition an object sits in.
 *
 * Answers `undefined` for an object that is in none, which is what
 * a freshly created one is.
 */
export function nodeParent(document: number, node: types.NodeHandle): types.NodeHandle | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    const $status = exports().otio_node_parent(document, $receiver, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the schema name an object serializes as, such as `"Clip"`.
 */
export function nodeSchemaName(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_node_schema_name(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns the schema version an object serializes as.
 */
export function nodeSchemaVersion(document: number, node: types.NodeHandle): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_node_schema_version(document, $receiver, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Sets an object's name.
 */
export function nodeSetName(document: number, node: types.NodeHandle, name: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(name);
    check(exports().otio_node_set_name(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Writes one object of a document as OTIO JSON.
 *
 * Upstream's `write_to_string` takes any object, not only a timeline, so this
 * does too.
 */
export function nodeToJson(document: number, node: types.NodeHandle, indent: number): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = indent;
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_node_to_json(document, $receiver, $arg0, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Restates an instant from one object's clock in another's.
 */
export function nodeTransformedTime(document: number, node: types.NodeHandle, time: values.RationalTimeLike, to: types.NodeHandle): values.RationalTime {
  const $stack = openStack();
  try {
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $receiver = $stack.node(node);
    const $arg1 = $stack.node(to);
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_node_transformed_time(document, $arg0, $receiver, $arg1, $out0));
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Restates a span from one object's clock in another's.
 */
export function nodeTransformedTimeRange(document: number, node: types.NodeHandle, range: values.TimeRangeLike, to: types.NodeHandle): values.TimeRange {
  const $stack = openStack();
  try {
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, range);
    const $receiver = $stack.node(node);
    const $arg1 = $stack.node(to);
    const $out0 = $stack.alloc(32, 8);
    check(exports().otio_node_transformed_time_range(document, $arg0, $receiver, $arg1, $out0));
    return types.readTimeRange($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether an object covers what is beneath it when its composition is
 * flattened.
 */
export function nodeVisible(document: number, node: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_node_visible(document, $receiver, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the sum of two times, at the higher of the two rates.
 */
export function rationalTimeAdd(self: values.RationalTimeLike, right: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, right);
    exports().otio_rational_time_add($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether two times are within `delta` of each other.
 */
export function rationalTimeAlmostEqual(self: values.RationalTimeLike, right: values.RationalTimeLike, delta: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, right);
    const $arg1 = delta;
    return exports().otio_rational_time_almost_equal($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the time rounded towards positive infinity.
 */
export function rationalTimeCeil(self: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    exports().otio_rational_time_ceil($sret, $receiver);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Orders two times: -1 if `left` is earlier, 1 if later, 0 if the same.
 *
 * A comparison involving a NaN has no answer, and reports 0.
 */
export function rationalTimeCompare(self: values.RationalTimeLike, right: values.RationalTimeLike): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, right);
    return exports().otio_rational_time_compare($receiver, $arg0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns how long it is from one instant to another, the end excluded.
 */
export function rationalTimeDurationFromStartEndTime(self: values.RationalTimeLike, endTimeExclusive: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, endTimeExclusive);
    exports().otio_rational_time_duration_from_start_end_time($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns how long it is from one instant to another, the end included.
 */
export function rationalTimeDurationFromStartEndTimeInclusive(self: values.RationalTimeLike, endTimeInclusive: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, endTimeInclusive);
    exports().otio_rational_time_duration_from_start_end_time_inclusive($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether two times are the same instant.
 *
 * Times at different rates that name the same instant compare equal; this is
 * upstream's `==`, not a field-by-field comparison. For that, use
 * `RationalTime#strictlyEqual`.
 */
export function rationalTimeEqual(self: values.RationalTimeLike, right: values.RationalTimeLike): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, right);
    return exports().otio_rational_time_equal($receiver, $arg0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the time rounded towards negative infinity.
 */
export function rationalTimeFloor(self: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    exports().otio_rational_time_floor($sret, $receiver);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Builds a time from a frame number at a rate.
 */
export function rationalTimeFromFrames(frame: number, rate: number): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $arg0 = frame;
    const $arg1 = rate;
    exports().otio_rational_time_from_frames($sret, $arg0, $arg1);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Builds a time from a number of seconds, at a rate of one.
 */
export function rationalTimeFromSeconds(seconds: number): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $arg0 = seconds;
    exports().otio_rational_time_from_seconds($sret, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Builds a time from a number of seconds at a rate.
 */
export function rationalTimeFromSecondsAtRate(seconds: number, rate: number): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $arg0 = seconds;
    const $arg1 = rate;
    exports().otio_rational_time_from_seconds_at_rate($sret, $arg0, $arg1);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a time from a `[-]HH:MM:SS.sss` time string at a rate.
 */
export function rationalTimeFromTimeString(timeString: string, rate: number): values.RationalTime {
  const $stack = openStack();
  try {
    const $arg0 = $stack.text(timeString);
    const $arg1 = rate;
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_rational_time_from_time_string($arg0, $arg1, $out0));
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a time from a `HH:MM:SS:FF` timecode at a rate.
 */
export function rationalTimeFromTimecode(timecode: string, rate: number): values.RationalTime {
  const $stack = openStack();
  try {
    const $arg0 = $stack.text(timecode);
    const $arg1 = rate;
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_rational_time_from_timecode($arg0, $arg1, $out0));
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a time is usable: both parts finite and the rate positive.
 */
export function rationalTimeIsValid(self: values.RationalTimeLike): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    return exports().otio_rational_time_is_valid($receiver) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the time with its sign flipped.
 */
export function rationalTimeNegate(self: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    exports().otio_rational_time_negate($sret, $receiver);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the same instant expressed at another rate.
 */
export function rationalTimeRescaledTo(self: values.RationalTimeLike, rate: number): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = rate;
    exports().otio_rational_time_rescaled_to($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the same instant expressed at another time's rate.
 */
export function rationalTimeRescaledToTime(self: values.RationalTimeLike, other: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, other);
    exports().otio_rational_time_rescaled_to_time($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the time rounded to the nearest whole value, halves away from zero.
 */
export function rationalTimeRound(self: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    exports().otio_rational_time_round($sret, $receiver);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether two times have the same value and the same rate.
 */
export function rationalTimeStrictlyEqual(self: values.RationalTimeLike, right: values.RationalTimeLike): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, right);
    return exports().otio_rational_time_strictly_equal($receiver, $arg0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the difference of two times, at the higher of the two rates.
 */
export function rationalTimeSubtract(self: values.RationalTimeLike, right: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, right);
    exports().otio_rational_time_subtract($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the frame number this time falls on.
 */
export function rationalTimeToFrames(self: values.RationalTimeLike): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    return exports().otio_rational_time_to_frames($receiver);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the frame number this time falls on at another rate.
 */
export function rationalTimeToFramesAtRate(self: values.RationalTimeLike, rate: number): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = rate;
    return exports().otio_rational_time_to_frames_at_rate($receiver, $arg0);
  } finally {
    $stack.close();
  }
}

/**
 * Writes a time as a timecode, rounding to the nearest frame first.
 */
export function rationalTimeToNearestTimecodeAt(self: values.RationalTimeLike, rate: number, dropFrame: types.DropFrame): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = rate;
    const $arg1 = types.encodeDropFrame(dropFrame);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_rational_time_to_nearest_timecode_at($receiver, $arg0, $arg1, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns the time in seconds.
 */
export function rationalTimeToSeconds(self: values.RationalTimeLike): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    return exports().otio_rational_time_to_seconds($receiver);
  } finally {
    $stack.close();
  }
}

/**
 * Writes a time as a `HH:MM:SS.sss` time string.
 */
export function rationalTimeToTimeString(self: values.RationalTimeLike): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_rational_time_to_time_string($receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Writes a time as a timecode at its own rate.
 */
export function rationalTimeToTimecode(self: values.RationalTimeLike): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_rational_time_to_timecode($receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Writes a time as a timecode at a given rate and drop-frame setting.
 */
export function rationalTimeToTimecodeAt(self: values.RationalTimeLike, rate: number, dropFrame: types.DropFrame): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = rate;
    const $arg1 = types.encodeDropFrame(dropFrame);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_rational_time_to_timecode_at($receiver, $arg0, $arg1, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns what this time's value would be at another rate.
 */
export function rationalTimeValueRescaledTo(self: values.RationalTimeLike, rate: number): number {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, self);
    const $arg0 = rate;
    return exports().otio_rational_time_value_rescaled_to($receiver, $arg0);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a document from the bytes of a file in some format.
 *
 * `options` may be null for the format's usual behaviour.
 */
export function readFromBytes(format: types.Format, data: Uint8Array, options: types.ReadOptions | undefined): number {
  const $stack = openStack();
  try {
    const $arg0 = types.encodeFormat(format);
    const $arg1 = $stack.bytes(data);
    const $arg2 = options === undefined ? 0 : $stack.record(types.writeReadOptions, types.sizeOfReadOptions, types.alignOfReadOptions, options);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_read_from_bytes($arg0, $arg1.pointer, $arg1.length, $arg2, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Reads a document from a file on disk in some format.
 */
export function readFromFile(format: types.Format, path: string, options: types.ReadOptions | undefined): number {
  const $stack = openStack();
  try {
    const $arg0 = types.encodeFormat(format);
    const $arg1 = $stack.text(path);
    const $arg2 = options === undefined ? 0 : $stack.record(types.writeReadOptions, types.sizeOfReadOptions, types.alignOfReadOptions, options);
    const $out0 = $stack.alloc(4, 4);
    check(exports().otio_read_from_file($arg0, $arg1, $arg2, $out0));
    return $stack.view.getUint32($out0, true);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the defaults, for a caller that wants to change one field.
 */
export function readOptionsDefault(): types.ReadOptions {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioReadOptions */
    exports().otio_read_options_default($sret);
    return types.readReadOptions($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a serializable collection: a group of objects with no timing.
 */
export function serializableCollectionNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_serializable_collection_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a stack.
 */
export function stackNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_stack_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the name of a status code, such as `"an `OtioError` whose status is `"ok"`"`.
 *
 * The string is static and needs no freeing.
 */
export function statusName(status: types.Status): string {
  const $stack = openStack();
  try {
    const $arg0 = types.encodeStatus(status);
    return readCString(exports().otio_status_name($arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Creates a time effect: an effect that alters timing and has no parameters.
 */
export function timeEffectNew(document: number, name: string | undefined, effectName: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = effectName === undefined ? 0 : $stack.text(effectName);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_time_effect_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether this range ends before another begins.
 */
export function timeRangeBeforeRange(self: values.TimeRangeLike, other: values.TimeRangeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    const $arg1 = epsilonS;
    return exports().otio_time_range_before_range($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether this range ends before an instant.
 */
export function timeRangeBeforeTime(self: values.TimeRangeLike, time: values.RationalTimeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $arg1 = epsilonS;
    return exports().otio_time_range_before_time($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether another range starts where this one does.
 */
export function timeRangeBeginsRange(self: values.TimeRangeLike, other: values.TimeRangeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    const $arg1 = epsilonS;
    return exports().otio_time_range_begins_range($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether this range starts at an instant.
 */
export function timeRangeBeginsTime(self: values.TimeRangeLike, time: values.RationalTimeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $arg1 = epsilonS;
    return exports().otio_time_range_begins_time($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the range pulled inside this one, where it lies outside it.
 */
export function timeRangeClampedRange(self: values.TimeRangeLike, other: values.TimeRangeLike): values.TimeRange {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(32, 8); /* OtioTimeRange */
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    exports().otio_time_range_clamped_range($sret, $receiver, $arg0);
    return types.readTimeRange($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the instant pulled inside the range, if it lies outside it.
 */
export function timeRangeClampedTime(self: values.TimeRangeLike, time: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    exports().otio_time_range_clamped_time($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether another range falls entirely inside this one.
 */
export function timeRangeContainsRange(self: values.TimeRangeLike, other: values.TimeRangeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    const $arg1 = epsilonS;
    return exports().otio_time_range_contains_range($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether an instant falls inside the range, the end excluded.
 */
export function timeRangeContainsTime(self: values.TimeRangeLike, time: values.RationalTimeLike): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    return exports().otio_time_range_contains_time($receiver, $arg0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the range with its duration lengthened.
 */
export function timeRangeDurationExtendedBy(self: values.TimeRangeLike, by: values.RationalTimeLike): values.TimeRange {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(32, 8); /* OtioTimeRange */
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, by);
    exports().otio_time_range_duration_extended_by($sret, $receiver, $arg0);
    return types.readTimeRange($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the instant just after the range's end.
 */
export function timeRangeEndTimeExclusive(self: values.TimeRangeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    exports().otio_time_range_end_time_exclusive($sret, $receiver);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the last instant the range covers.
 */
export function timeRangeEndTimeInclusive(self: values.TimeRangeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    exports().otio_time_range_end_time_inclusive($sret, $receiver);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the smallest range covering both of two ranges.
 */
export function timeRangeExtendedBy(self: values.TimeRangeLike, other: values.TimeRangeLike): values.TimeRange {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(32, 8); /* OtioTimeRange */
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    exports().otio_time_range_extended_by($sret, $receiver, $arg0);
    return types.readTimeRange($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether another range ends where this one does.
 */
export function timeRangeFinishesRange(self: values.TimeRangeLike, other: values.TimeRangeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    const $arg1 = epsilonS;
    return exports().otio_time_range_finishes_range($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether this range ends at an instant.
 */
export function timeRangeFinishesTime(self: values.TimeRangeLike, time: values.RationalTimeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    const $arg1 = epsilonS;
    return exports().otio_time_range_finishes_time($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Builds a range from its start and the instant after its end.
 */
export function timeRangeFromStartEndTime(startTime: values.RationalTimeLike, endTimeExclusive: values.RationalTimeLike): values.TimeRange {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(32, 8); /* OtioTimeRange */
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, startTime);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, endTimeExclusive);
    exports().otio_time_range_from_start_end_time($sret, $arg0, $arg1);
    return types.readTimeRange($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Builds a range from its start and its last instant.
 */
export function timeRangeFromStartEndTimeInclusive(startTime: values.RationalTimeLike, endTimeInclusive: values.RationalTimeLike): values.TimeRange {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(32, 8); /* OtioTimeRange */
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, startTime);
    const $arg1 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, endTimeInclusive);
    exports().otio_time_range_from_start_end_time_inclusive($sret, $arg0, $arg1);
    return types.readTimeRange($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether two ranges share more than a single boundary instant.
 */
export function timeRangeIntersects(self: values.TimeRangeLike, other: values.TimeRangeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    const $arg1 = epsilonS;
    return exports().otio_time_range_intersects($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a range is usable: valid times and a duration of at least
 * zero.
 */
export function timeRangeIsValid(self: values.TimeRangeLike): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    return exports().otio_time_range_is_valid($receiver) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether this range ends exactly where another begins.
 */
export function timeRangeMeets(self: values.TimeRangeLike, other: values.TimeRangeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    const $arg1 = epsilonS;
    return exports().otio_time_range_meets($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether two ranges share any time at all.
 */
export function timeRangeOverlapsRange(self: values.TimeRangeLike, other: values.TimeRangeLike, epsilonS: number): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, other);
    const $arg1 = epsilonS;
    return exports().otio_time_range_overlaps_range($receiver, $arg0, $arg1) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether an instant falls inside the range, the ends included.
 */
export function timeRangeOverlapsTime(self: values.TimeRangeLike, time: values.RationalTimeLike): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    return exports().otio_time_range_overlaps_time($receiver, $arg0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns the transform applied to a span.
 */
export function timeTransformAppliedToRange(self: values.TimeTransformLike, range: values.TimeRangeLike): values.TimeRange {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(32, 8); /* OtioTimeRange */
    const $receiver = $stack.record(types.writeTimeTransform, types.sizeOfTimeTransform, types.alignOfTimeTransform, self);
    const $arg0 = $stack.record(types.writeTimeRange, types.sizeOfTimeRange, types.alignOfTimeRange, range);
    exports().otio_time_transform_applied_to_range($sret, $receiver, $arg0);
    return types.readTimeRange($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the transform applied to an instant.
 */
export function timeTransformAppliedToTime(self: values.TimeTransformLike, time: values.RationalTimeLike): values.RationalTime {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(16, 8); /* OtioRationalTime */
    const $receiver = $stack.record(types.writeTimeTransform, types.sizeOfTimeTransform, types.alignOfTimeTransform, self);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    exports().otio_time_transform_applied_to_time($sret, $receiver, $arg0);
    return types.readRationalTime($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Returns the transform applied to another transform.
 */
export function timeTransformAppliedToTransform(self: values.TimeTransformLike, other: values.TimeTransformLike): values.TimeTransform {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(32, 8); /* OtioTimeTransform */
    const $receiver = $stack.record(types.writeTimeTransform, types.sizeOfTimeTransform, types.alignOfTimeTransform, self);
    const $arg0 = $stack.record(types.writeTimeTransform, types.sizeOfTimeTransform, types.alignOfTimeTransform, other);
    exports().otio_time_transform_applied_to_transform($sret, $receiver, $arg0);
    return types.readTimeTransform($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Clears where a timeline begins.
 */
export function timelineClearGlobalStartTime(document: number, node: types.NodeHandle): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    check(exports().otio_timeline_clear_global_start_time(document, $receiver));
  } finally {
    $stack.close();
  }
}

/**
 * Returns where a timeline begins, such as `01:00:00:00`.
 *
 * Answers `undefined` for a timeline that does not say.
 */
export function timelineGlobalStartTime(document: number, node: types.NodeHandle): values.RationalTime | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(16, 8);
    const $status = exports().otio_timeline_global_start_time(document, $receiver, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a timeline, with an empty stack named `"tracks"` already in it.
 *
 * Upstream's `Timeline()` builds that stack in its constructor, and its own
 * tests append to a fresh timeline's tracks without making one first, so a
 * timeline from here arrives the same way rather than leaving every binding
 * to invent the difference. Replace it with `Timeline#tracks` to
 * use a stack of your own; the one built here is thrown away with the
 * document.
 */
export function timelineNew(document: number, name: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_timeline_new(document, $arg0, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets where a timeline begins.
 */
export function timelineSetGlobalStartTime(document: number, node: types.NodeHandle, time: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, time);
    check(exports().otio_timeline_set_global_start_time(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the stack holding a timeline's tracks.
 *
 * The stack's parent is set to the timeline, as upstream's does.
 *
 * Passing `undefined` puts a fresh empty stack there rather than
 * nothing, because that is what upstream's setter does: its own
 * `test_timeline.py` sets `tracks` to `None` and then asserts that
 * `tl.tracks` is still a `Stack`. A timeline with no tracks at all is not a
 * thing a caller can reach through upstream's API, so it is not one they can
 * reach through this one.
 *
 * Whatever stack was there is not destroyed. It stays in the document,
 * parentless, so it can be put somewhere else; dropping it is a separate
 * `Node#remove` call. That is the same bargain
 * `Composition#detachChild` makes, and leaving its parent pointing at
 * the timeline instead would mean an object claiming a parent that has
 * disowned it.
 */
export function timelineSetTracks(document: number, node: types.NodeHandle, tracks: types.NodeHandle | undefined): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = tracks === undefined ? $stack.noneNode() : $stack.node(tracks);
    check(exports().otio_timeline_set_tracks(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the stack holding a timeline's tracks.
 *
 * Answers `undefined` for a timeline that has none.
 */
export function timelineTracks(document: number, node: types.NodeHandle): types.NodeHandle | undefined {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    const $status = exports().otio_timeline_tracks(document, $receiver, $out0);
    if ($status === NO_VALUE) {
      return undefined;
    }
    check($status);
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns what a track carries, such as `"Video"` or `"Audio"`.
 */
export function trackKind(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_track_kind(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Creates a track. `kind` may be null, which means `"Video"`, as upstream's
 * default does.
 */
export function trackNew(document: number, name: string | undefined, kind: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = kind === undefined ? 0 : $stack.text(kind);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_track_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets what a track carries.
 */
export function trackSetKind(document: number, node: types.NodeHandle, kind: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(kind);
    check(exports().otio_track_set_kind(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns whether a transition is applied.
 */
export function transitionEnabled(document: number, node: types.NodeHandle): boolean {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(1, 1);
    check(exports().otio_transition_enabled(document, $receiver, $out0));
    return $stack.view.getUint8($out0) !== 0;
  } finally {
    $stack.close();
  }
}

/**
 * Returns how far a transition reaches into the item before it.
 */
export function transitionInOffset(document: number, node: types.NodeHandle): values.RationalTime {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_transition_in_offset(document, $receiver, $out0));
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Creates a transition. Its offsets start at zero.
 */
export function transitionNew(document: number, name: string | undefined, transitionType: string | undefined): types.NodeHandle {
  const $stack = openStack();
  try {
    const $arg0 = name === undefined ? 0 : $stack.text(name);
    const $arg1 = transitionType === undefined ? 0 : $stack.text(transitionType);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_transition_new(document, $arg0, $arg1, $out0));
    return types.readNodeHandle($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Returns how far a transition reaches into the item after it.
 */
export function transitionOutOffset(document: number, node: types.NodeHandle): values.RationalTime {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(16, 8);
    check(exports().otio_transition_out_offset(document, $receiver, $out0));
    return types.readRationalTime($stack.view, $out0);
  } finally {
    $stack.close();
  }
}

/**
 * Sets whether a transition is applied.
 */
export function transitionSetEnabled(document: number, node: types.NodeHandle, enabled: boolean): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = enabled ? 1 : 0;
    check(exports().otio_transition_set_enabled(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets how far a transition reaches into the item before it.
 */
export function transitionSetInOffset(document: number, node: types.NodeHandle, offset: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, offset);
    check(exports().otio_transition_set_in_offset(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets how far a transition reaches into the item after it.
 */
export function transitionSetOutOffset(document: number, node: types.NodeHandle, offset: values.RationalTimeLike): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.record(types.writeRationalTime, types.sizeOfRationalTime, types.alignOfRationalTime, offset);
    check(exports().otio_transition_set_out_offset(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Sets the kind of transition.
 */
export function transitionSetType(document: number, node: types.NodeHandle, transitionType: string): void {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $arg0 = $stack.text(transitionType);
    check(exports().otio_transition_set_type(document, $receiver, $arg0));
  } finally {
    $stack.close();
  }
}

/**
 * Returns the kind of transition, such as `"SMPTE_Dissolve"`.
 */
export function transitionType(document: number, node: types.NodeHandle): string {
  const $stack = openStack();
  try {
    const $receiver = $stack.node(node);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_transition_type(document, $receiver, $out0));
    return readBuffer($out0, "text");
  } finally {
    $stack.close();
  }
}

/**
 * Returns the library's version, as `"MAJOR.MINOR.PATCH"`.
 *
 * The string is static and needs no freeing.
 */
export function version(): string {
  const $stack = openStack();
  try {
    return readCString(exports().otio_version());
  } finally {
    $stack.close();
  }
}

/**
 * Returns the defaults, for a caller that wants to change one field.
 */
export function writeOptionsDefault(): types.WriteOptions {
  const $stack = openStack();
  try {
    const $sret = $stack.alloc(24, 8); /* OtioWriteOptions */
    exports().otio_write_options_default($sret);
    return types.readWriteOptions($stack.view, $sret);
  } finally {
    $stack.close();
  }
}

/**
 * Writes a document as the bytes of a file in some format.
 *
 * The buffer is NUL-terminated, so a text format's output can be used as a C
 * string; `len` is what matters for a binary one.
 */
export function writeToBytes(document: number, format: types.Format, options: types.WriteOptions | undefined): Uint8Array {
  const $stack = openStack();
  try {
    const $arg0 = types.encodeFormat(format);
    const $arg1 = options === undefined ? 0 : $stack.record(types.writeWriteOptions, types.sizeOfWriteOptions, types.alignOfWriteOptions, options);
    const $out0 = $stack.alloc(8, 4);
    check(exports().otio_write_to_bytes($arg0, document, $arg1, $out0));
    return readBuffer($out0, "bytes");
  } finally {
    $stack.close();
  }
}

/**
 * Writes a document to a file on disk in some format.
 */
export function writeToFile(document: number, format: types.Format, path: string, options: types.WriteOptions | undefined): void {
  const $stack = openStack();
  try {
    const $arg0 = types.encodeFormat(format);
    const $arg1 = $stack.text(path);
    const $arg2 = options === undefined ? 0 : $stack.record(types.writeWriteOptions, types.sizeOfWriteOptions, types.alignOfWriteOptions, options);
    check(exports().otio_write_to_file($arg0, document, $arg1, $arg2));
  } finally {
    $stack.close();
  }
}

